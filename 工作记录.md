# 轻MES 工作记录

> 最后更新：2026-08-14（PanelX 平台对接实测通过：SDK 加载→登录弹窗→平台数据渲染，修复假登录态）

- [x] PanelX 平台对接（2026-08-14，实测 demo.kwaidoo.com）
  - 验证：VITE_PANELX=true 起 dev server → preload.js 拉取 SDK 成功 → 无会话进面板自动弹「登录 PanelX 平台」→ admin/123456 登录 → MANU_ORDER 映射 SdkTest_IML_00002 渲染「测试流程面板」18 条真实数据（E2E 截图 docs/ref/mes-live/panelx-live-01.png + panelx-live-e2e.json + panelx-diag.json）
  - 修复：假登录态（localStorage 残留失效 token → isAuthenticated()=true → 平台 API 失败但页面静默空数据）；新增 `platformCall` 统一包装（非 200 → 归一错误；认证失效 → 「PanelX 平台未登录」），PanelxList.load catch 检测「未登录」→ 弹 PanelxLogin
  - 面板码：PANEL_MAP 映射（MANU_ORDER→SdkTest_IML_00002），其余透传；平台字段/工具栏经 adaptPanelConfig/adaptMeta 适配
  - 范围：仅表单引擎走平台，门户壳仍 mock；报表/看板/返修台为前端派生，平台模式不适用

- [x] 审批流实现（2026-08-14，依据 T+ 实测 15 面板）
  - 范围：14 个面板（APPROVAL_PANELS：SO_ORDER/6 库存单据/MANU_ORDER/PROCESS_REPORT/INIT_AP/INIT_AR/INIT_BALANCE/BOM/ROUTE）；REWORK_REPORT 为派生只读报表，不适用审批（已说明）
  - 状态机（engine.callButton mock）：提交审批(草稿→审批中+审批状态=待审批) → 审批通过(审批中→已审核+审批状态=已审批+审批人/审批时间) / 驳回审批(审批中→草稿+审批状态=已驳回)；弃审清空审批状态
  - 配置注入（applyApprovalConfig）：getPanelConfig mock 时给审批面板补 工具栏「审批」组（提交审批/审批通过/驳回审批，插在 审核 组后）、单据状态选项补「审批中」、panelButtons 补按钮
  - 前端（PanelxList.vue）：当前行 审批状态=已审批 → 表格左上角斜置绿色「已审批」印章角标（.approved-stamp）；已审批行浅绿底色（row-approved）；审批按钮按状态禁用（提交审批仅草稿、审批通过/驳回仅审批中）
  - 种子数据：SO×3/RK/IO/MO×2/MR 共 8 行预置 审批状态=已审批 便于演示角标
  - E2E 验证（无头 Edge 9223 + localhost:5173，approval-e2e.json）：①草稿行提交审批可用 ②提交后审批通过/驳回变可用 ③审批通过后角标「已审批」显示+行高亮 ④弃审→再提交正常 ⑤驳回后角标消失回草稿；BOM/ROUTE/INIT×3/MANU 审批组注入抽查全部通过；构建通过

- [x] T+ 审批面板调研（2026-08-14，实测 h2t.chanjet.com [012007] 轻MES）
  - 方法：门户 CreateTab API 打开 20 面板 → 抓取服务端 Voucher.aspx HTML → 提取工具栏按钮（Audit/Unaudit/ViewWFProgress）与标志（HasAuditProcess/HasWFAuditProcess/IsNeedAudit）；存档 `docs/design/T+审批面板调研-20260814.md` + `%TEMP%\pptr\approval-flags.json`
  - 结论①有审核环节（15 面板）：销售订单、6 库存单据、生产加工单、工序汇报单、返修工序汇报单、期初暂估/销售出库单、库存期初余额、物料清单、工艺路线（HasAuditProcess=True 或按钮实测）
  - 结论②审批流（HasWFAuditProcess）：演示账套 12 单据全部 False 未启用；「审批情况」按钮为工具栏固定渲染，未启用时不可用；审批流需在 单据设计→审批流 配置
  - 结论③强制审核（IsNeedAudit=True）：生产加工单/工序汇报单/返修工序汇报单（未审核禁止后续业务，如末工序自动入库需审核后触发）
  - 结论④无需审核：基础档案 13 个（BaseInfoList）、报表 40 个、系统设置、生产看板、返修工作台、方案中心
  - 启发：轻MES mock 单据状态机（草稿→已审核）与 T+ 一致；后续可对 IsNeedAudit 单据补 未审核禁止生单 约束，或加审批流配置面板

- [x] 生产/销售报表与专属视图（2026-08-14，31 占位面板收官）
  - 参考：`docs/design/T+轻MES生产加工单-真实面板设计.md`（真实报表菜单：产成品明细表/统计表/工序统计表/执行表/工序汇报单明细表·统计表/工资核算单明细表·统计表/生产看板）+ 现有 MANU_ORDER/PROCESS_REPORT/SO 种子数据
  - 销售订单报表 4（配置驱动）：SALES_ORDER_DETAIL 明细表（24 列）/ SALES_ORDER_STATS 统计表（按客户+存货分组）/ SALES_ORDER_EXEC 执行表（订单 vs 已出库 SALE_OUT + 已生产 MANU 联动，出库执行率/生产进度/未执行）/ SALES_ORDER_PROGRESS 生产进度表（订单→加工单→汇报联动）
  - 生产报表 3（配置驱动）：MANU_ORDER_DETAIL 明细表（产成品明细行 20 列）/ MANU_ORDER_STATS 统计表（按产品分组：加工单数/计划/累计汇报/完工/进度）/ MANU_PROC_STATS 工序统计表（按工序分组：加工单数/计划/金额/完工状态分布）
  - 车间报表 5（配置驱动）：PROC_DETAIL 工序明细表（汇报行 23 列）/ PROC_STATS 工序统计表（按工序+班组+工人，合格率）/ SALARY_DETAIL 工资明细表（计件数量/工价/计件金额）/ SALARY_STATS 工资统计表（按工人+班组）/ REWORK_REPORT 返修工序汇报单（MANU 工序待返修数量 本序/他序 派生）
  - 专属视图 3：`ManufactureBoard.vue` 生产看板（KPI 5 卡 + 车间工序分布 + 加工单进度条，engine `getProdBoard()` mock 派生）、`ReworkDesk.vue` 返修工作台（任务卡片池 + 开始返修/完成返修 联动 REWORK_REPORT 缓存行）、`SolutionCenter.vue` 方案中心（行业方案/应用市场静态卡片）；router 显式路由 + ModuleView 兜底排除
  - ⚠️ 踩坑修复：MOCK_ROWS 无「单据编号」字段（仅 编号/锭号）导致 MANU_ORDER_DETAIL/REWORK_REPORT 单号空 → 报表行回退 `r['单据编号'] || r['锭号'] || r['编号']`
  - E2E 验证（无头 Edge 9223 + localhost:5173，15/15 通过，截图 docs/ref/mes-live/report2-*.png + report2-e2e.json）：12 报表行数正确（SO 7 行/统计 6 行/加工单明细 8 行/工序统计 10 行/返修 3 行等）；返修工作台「开始返修」交互后状态 待返修→返修中 生效；构建通过
  - ✅ 至此 31 个占位面板全部开发完成（16 库存报表 + 12 生产/销售报表 + 3 专属视图）；⚠️ 已知事项：报表面板 panel_config 尚未同步 init.sql（后端模式缺配置，待生成器统一补齐）

- [x] 库存核算报表 16 面板（2026-08-14，对齐真实 T+ 报表实测）
  - 参考：`docs/ref/tplus-live/库存核算报表-20260814/`（ST4001/4002/4004/4005/4006/4008 明细表、ST6001/6002/6004/6011/6012/6014 统计表、ST3030 成本维护、ST9001 库存状况、ST6061 收发存汇总、ST9002 库存台账 实测结构）+ `rpt-summary.md` + `T+库存核算-真实面板设计.md`
  - 引擎扩展（engine.js）：新增 `reportPanel()` 只读报表工厂（metadata.readonly=true、无表单页/内联新增行、工具栏照 T+：查询/打印/更多 或成本维护专用 查找/更新单据/设置/批量取成本/汇总/更多）、`REPORT_CONFIGS` 16 面板配置（列结构对齐实测 report-structure.json）、报表行派生（`invReportRows` 表头+明细平铺补齐 编码列/部门/制单人/审核人、`groupSum` 分组汇总、`stockSummaryRows` 收发存期初/入/出/末、`stockLedgerRows` 台账期初+流水滚动结存、`INV_DICT`/`PARTNER_DICT` 从基础档案 seed 建字典）、`REPORT_ROWS_CACHE` 会话内缓存（编辑/保存一致）
  - 查询区：单据日期/单据编号/业务类型 + 参照字段（仓库→WH、供应商/客户→PARTNER、存货/材料→INV，满足约束十一-1 能对应必须对应）
  - PanelxList.vue：readonly 面板跳过内联新增行与双击开表单；工具栏「查询」直接刷新（T+ 报表工具栏）；合计行改为按数值类型识别（兼容报表列名如 实收数量(主单位)/期初金额）
  - menus.js：库存核算 16 个占位菜单挂 `/panelx/list/XXX`（panelCode：PURCHASE_IN_DETAIL…STOCK_LEDGER）；UNIMPLEMENTED 补 发送邮件/更新单据/设置/批量取成本/汇总维度设置/汇总导出/汇总导入/模板下载
  - E2E 验证（无头 Edge 9223 + localhost:5173，16/16 通过，截图 docs/ref/mes-live/report-*.png + report-e2e.json）：
    - STOCK_LEDGER 20 行（14 期初 + 6 流水，滚动结存）；COST_MAINTAIN 10 行（六单明细行）；SALE_OUT_STATS 合计 300；STOCK_STATUS 现存量与收发存期末一致（铝棒 500=期初500+入200-出200）
  - ⚠️ 已知事项：16 个报表面板的 panel_config 尚未同步 init.sql（后端模式缺配置，待生成器统一补齐）；销售订单报表 4 + 生产 4 + 车间 6 + 方案中心 1 共 15 个占位面板待开发

- [x] 三单据实测复刻（2026-08-14，实测 T+ 演示环境 tplusdemo12862）
  - 实测：t.chanjet.com 立即体验 → 新制造/轻MES；菜单码 **#ST1021 销售出库单** / **#ST1002 产成品入库单**（智能供应链→库存核算→单据）/ **#MR10 工序汇报单**（新生产→智慧车间→单据，sysId=mr）
  - 原始抓取存档：`docs/ref/tplus-live/三个单据-20260814/`（每单 01-list.json/txt/png + 02-form.json/txt/png）；设计文档：`docs/design/T+销售出库单-产成品入库单-工序汇报单-真实面板设计.md`
  - **SALE_OUT 销售出库单升级**（对齐真实 ST1021 字段序）：表头补 出库类别/客户编码/客户简称/部门/门店/验货人/验货日期/发货人/发货日期/项目/收货仓库/来源单据/外部单据号/来源单号/销售订单号；参照字段：客户/客户编码/结算客户→PARTNER（客户弹窗带出 客户编码/结算客户/客户简称/部门）、部门→DEPT、经手人/验货人/发货人→EMP、项目→PROJ、仓库/收货仓库→WH、明细 存货名称→INV（带出 编码/规格/单位）、明细 仓库→WH；查询区同步
  - **FINISH_IN 产成品入库单升级**：表头补 经手人/验货人/验货日期/项目/对方仓库/来源单据/外部单据号/来源单号/工序汇报单号/销售订单号；参照：经手人/验货人→EMP、项目→PROJ、仓库/对方仓库→WH、明细 产品名称→INV（带出 规格/单位/图号）、明细 仓库→WH
  - **PROCESS_REPORT 工序汇报单（全新，单号 MR-yyyy-MM-####）**：表头 24 字段（含 末工序按合格数量自动入库/检验工序自动报检/报废自动入库/保持汇报数量不变/返修汇报/返修模式/生单模式 状态位）、明细 61 列（加工单号/产品/工艺/工序编码→OP 参照/班组→TEAM/工人→EMP/设备→EQUIP/工作中心→WC/委外供应商→PARTNER/报工数量/合格/不合格/工废/料废/工价/计时计件金额/委外链/工时/金额 calc 链）、班组成员展开子表 13 列、汇总页签、工具栏 12 组 T+ 分组（含 工序条码录入 Alt+R）、选单-选生产加工单（**detailRows 配置带出工序行**，对齐 T+ 选单前提：已审核未中止）；表头 产品编码→INV、客户/委外供应商→PARTNER、部门→DEPT、经手人→EMP、报废预入仓库→WH
  - **引擎扩展**：selectConfig 支持 `detailRows`（选单带出指定明细行，如选生产加工单→待汇报工序行）；PanelxForm 选单增强：**按单据编号去重**（detailRows 配置时选单粒度=单据，避免一张多产品单显示多行）+ **多选单据时对每个选中单取工序明细合并**（勾选 2 张已审核 MO → 5 条工序行，已验证）；组件保持通用（mock 下从 MANU_ORDER 原始行取工序明细并补齐单据头产品信息，真实模式退化为默认行）
  - 演示数据：MOCK_ROWS 追加 MO-2026-08-0010（已审核，CP002 铝板 6061，2 工序行）供选单多选演示
  - ⚠️ 踩坑：**PowerShell 双引号字符串做代码替换会把 `$变量` 插值清空**（如 `$selectRows` → 空），曾导致 PanelxForm.vue confirmSelect 区域错位污染（路由懒加载编译失败→页面回退 dashboard，表单打不开）；修复后统一改用 Node 脚本（readFileSync + IndexOf 替换）做代码文本替换
  - menus.js：智慧车间→单据→工序汇报单 挂 `/panelx/list/PROCESS_REPORT`（原占位路由）
  - init.sql：panel_config 追加 SALE_OUT/FINISH_IN/PROCESS_REPORT（从 engine.js 提取配置 JSON 生成插入，**顺带修复 MANU/SO 元组行尾缺失 `),` 的结构问题**）；⚠️ 已知事项：基础档案/库存其余面板的 panel_config 目前不在 init.sql（仅 MANU/SO + 本批 3 个），后端模式缺配置，待生成器统一补齐
  - E2E 验证（无头 Edge 9223 + localhost:5173，全部通过，截图 docs/ref/mes-live/tplus-e2e-01~05.png）：
    - SALE_OUT 新增表单 24 表头字段（对齐真实字段序）；客户参照弹窗 6 列，导入 华东铝业 → 带出 结算客户=华东铝业、部门=销售一部（客户编码存 KH001 显示为名称）；明细存货参照导入 铝棒 Φ80 → 带出 规格型号/计量单位
    - FINISH_IN 新增表单 18 表头字段（含 经手人/验货人/对方仓库/工序汇报单号）；明细产品参照导入 铝板 6061 → 带出 规格型号/计量单位
    - PROCESS_REPORT 列表 3 数据行（MR-2026-08-0001/0002）+ 内联新增行；新增表单 24 表头字段；选单→选生产加工单 带出 **3 条待汇报工序行**（加工单号 MO-2026-08-0007、产品编码 CP003、产品名称 铝型材-散热片、工序 PX001 下料、报工数量 340）

- [x] 参照字段弹窗选择（2026-08-14，全部面板通用）
  - 交互：参照字段（`dataType: '参照'` + `refPanel`）点击 → **RefPickDialog 弹窗展示引用面板数据列表**（列 = `refColumns` 配置 || 引用面板网格列）→ 勾选行 → 确定导入：值写入 `refField`，`refMap` 把选中行的关联字段带出到本面板，`refMulti` 支持表头多值（值顿号连接）
  - **多行一次导入（2026-08-14 交互优化）**：任意参照字段可勾选多行；**明细行参照（mode=detail）每个选中项生成一条明细行**（当前行填第一行 + 其余追加，含 refMap 带出）；表头参照按 refMulti 决定（多值顿号连接 / 单值取第一行）；确认后弹窗自动关闭（防重复点击、按钮显示选中行数）；提示文案按场景区分（明细"每行生成一条明细" / 表头多值"顿号连接" / 表头单值"仅导入第一行"）
  - 验证：SO 明细存货名称勾选 2 行 → 确认 → 2 行明细（铝棒 Φ80/CP001/Φ80×3000 + 铝板 6061/CP002/1500×3000×2）+ 弹窗关闭；表头参照/选单多选合并（5 行）回归通过
  - 覆盖范围：表单表头 / 明细行 / 新增弹窗（NewVoucherDialog）/ 列表内联新增行；列表查询区的参照字段渲染为动态下拉（`fieldOptions`）
  - **约束表述修正（十一-1）**：「禁止手写静态 options」→「**能对应的字段必须对应上**」——语义上对应基础档案的字段一律用引用字段关联，仅当基础档案无对应语义档案时才允许手写 options
  - 首批字段对应（engine.js mock 配置 + init.sql panel_config 种子同步）：
    - SO_ORDER 表头「客户」→ PARTNER（refField 往来单位名称，带出 客户编码/结算客户）
    - SO_ORDER 明细「存货名称」→ INV（带出 存货编码/规格型号/品牌→存货名称.品牌/计量单位→销售单位）
    - MANU_ORDER 表头「客户编码」→ PARTNER（refField 往来单位编码，带出 客户/生产订单客户）
    - MANU_ORDER 材料明细「材料编码」→ INV（filter 属性=外购，带出 材料名称/规格型号/计量单位）
    - MANU_ORDER 工序明细「工序编码」→ OP（filter 是否停用=false，带出 工序名称/默认车间→生产车间/备注→工序备注）
  - 新增组件 `RefPickDialog.vue`；engine.js 新增 `queryRefRows`（mock：本地 seed + filter 精确 + keyword 模糊；真实/平台：queryFormDataList）、`refColumns`、`refPanelName`、`refLabelOf`；buildMeta 输出 `ref`（panel/field/display/filter/map/multi/columns）
  - 踩坑：edit 工具对 `frontend/src/views/panelx/` 目录 ReplaceFileW EIO——用 Node 批量替换脚本回退（`%TEMP%` 附近 dsh-edit 目录，替换对唯一性校验）；文件可能被多会话并发修改，改前先核对 hash/行数
  - E2E 验证（无头 Edge 9223 + localhost:5173，全部通过，截图 docs/ref/mes-live/ref-pick-01~06）：
    - SO 表头客户参照：弹窗列=refColumns 配置 6 列，勾选导入后 客户=华东铝业、带出 客户编码=KH001、结算客户=华东铝业
    - SO 明细存货参照：弹窗 7 列，导入后 存货名称=铝棒 Φ80、带出 存货编码=CP001/规格型号=Φ80×3000/品牌=轻铝/销售单位=件
    - MANU 工序参照：弹窗 7 列，导入后 工序编码=PX001（显示 下料）、带出 工序名称=下料/生产车间=熔铸车间
  - 踩坑：meta 字段的参照信息嵌套在 `f.ref`（buildMeta 输出），`normRef` 需归一 `f.ref` 与平铺字段两种形态（否则 refPanel 解析为 undefined 兜底到 MOCK_CONFIG）；PARTNER 基础档案的「结算客户」是布尔标记（是否结算客户），refMap 带出名称须用「往来单位名称」

- [x] 新增方式改造（2026-08-14，全部面板通用）
  - 内联新增：列表页表格末尾常驻空白新增行，双击单元格进入编辑（按字段类型渲染 文本/下拉/日期/数字/是否 控件，双击后自动聚焦），编辑完点行尾「保存」自动落库（表头+首明细行按面板类型组装），「取消」丢弃；双击数据行仍打开表单编辑（不冲突）
  - 弹窗新增：工具栏「新增」不再跳转独立页面，改为 NewVoucherDialog 弹窗（表头字段 3 列网格 + 明细子表格增删行 + 计算链 + 必填校验 + 保存/取消），保存后列表刷新；query?new=1 入口同样弹窗
  - 新组件 NewVoucherDialog.vue；PanelxList.vue 增加 inlineRow/inlineCol/mergedList/startInline/inlineSave/onCellDblClick
  - E2E 验证（无头 Edge 9223）：内联新增 DEPT-011（D99/测试部门99 保存成功）、弹窗新增 D98、双击打开表单、INIT_BALANCE 弹窗（表头+明细增行）全部通过；截图 docs/ref/mes-live/40-41
  - 踩坑：el-table-column 无 :prop 时 cell-dblclick 的 column.property 为 undefined（双击不激活）——补 :prop；headless 环境 autofocus 不可靠——组件内 nextTick 显式聚焦

## 一、项目概况

参照畅捷通 T+ 门户形态的网页端轻量 MES 系统。

- 名称：轻MES（Light MES）
- 位置：`F:\INCER\light-mes\`
- 技术栈：Vue3 + Element Plus + Pinia + Vue Router（前端）｜Spring Boot 3.5 + MyBatis-Plus + MySQL + JWT（后端）
- 开发环境：Node v24 / Java 24（`D:\Program Files\Java\jdk-24`，`C:\Program Files\Java\jdk-24` 为隐藏联接）/ 便携 Maven 3.9.9（`tools\apache-maven-3.9.9`，阿里云镜像 `tools\settings.xml`）；`tools\detect-jdk.bat` 自动探测 JDK（17+），build.bat/run.bat 无需手动改路径

## 二、菜单结构（对齐畅捷通 T+，已定稿）

```
我的桌面
智能供应链
├─ 销售管理
│  ├─ 单据：销售订单
│  ├─ 明细表：销售订单明细表
│  ├─ 统计表：销售订单统计表
│  ├─ 执行表：销售订单执行表
│  └─ 更多：销售订单生产进度表
└─ 库存核算
   ├─ 单据：采购入库单、产成品入库单、其他入库单、销售出库单、材料出库单、其他出库单
   ├─ 明细表：各出入库单据明细表 ×6
   ├─ 统计表：各出入库单据统计表 ×6
   ├─ 成本核算：成本手工维护
   └─ 库存账表：库存状况表、收发存汇总表、库存台账
新生产
├─ 生产管理
│  ├─ 单据：生产加工单、生产加工单工序统计表
│  └─ 报表：生产加工单明细表、生产加工单统计表、生产看板
└─ 智慧车间
   ├─ 单据：工序汇报单、返修工序汇报单、返修工作台
   └─ 报表：工序明细表、工序统计表、工资明细表、工资统计表
基础设置
├─ 基础设置
│  ├─ 基本信息：部门、存货、物料清单、工艺路线、仓库、工序
│  └─ 价格信息：存货价格本
├─ 系统管理
│  ├─ 基本设置：选项设置、看板授权
│  ├─ 单据档案设置：单据设计、编码设置、打印管理
│  ├─ 消息中心及预警设置：预警设置
│  └─ 账套管理及工具：任务管理、大屏相关管理
└─ 初始化
   ├─ 期初余额：库存期初余额
   └─ 期初单据：期初暂估入库单、期初销售出库单
TOP应用
└─ 方案中心
```

共 45 个菜单节点，前端配置源：`frontend/src/config/menus.js`，后端种子：`backend/src/main/resources/db/init.sql`。

## 三、导航交互规范（已定稿）

1. **一级目录**：左侧竖向排列（我的桌面/智能供应链/新生产/基础设置/TOP应用）
2. 点击一级 → **二级折叠在其下**（手风琴展开/收起）
3. 悬停二级 → 右侧**弹出卡片**（与悬停行对齐，移出导航区自动收起）
4. 卡片内：**三级分组（单据/明细表/统计表…）横向并排分列**，无 tab 切换、全部展示
5. 分组标题在列顶部，菜单项在各列内竖向排列
6. 点击菜单项 → 打开路由 + 新增页签；当前路由所属一级自动展开并高亮

## 四、已完成

- [x] 门户壳：TopBar（Logo/工厂切换/菜单搜索/待办·消息·预警角标/暗色换肤/全屏/用户下拉）
- [x] 三级导航（LeftNav，按第三节规范实现）
- [x] 多页签（打开/关闭/关闭其他/关闭全部/右键菜单/keep-alive）
- [x] 登录页 + 路由守卫
- [x] 我的桌面（KPI/生产进度/待办/快捷入口）
- [x] 45 个模块占位页（含各模块规划要点文案）
- [x] 后端骨架：登录（BCrypt+JWT）/菜单树/工厂列表/角标接口；MySQL 初始化脚本（用户+菜单+工厂种子数据）；`build.bat`/`run.bat` 一键构建启动
- [x] 前端 mock 模式（默认，`VITE_MOCK=false` 切真实接口，vite 已配 `/api` 代理到 8080）
- [x] T+ 对照补齐：左菜单顶部快捷区（展开/折叠菜单、单据查询、新增单据）、侧边栏折叠模式与菜单平铺模式、界面设置（TopBar 用户下拉）、工作台设置（快捷入口/KPI/进度/待办显隐）、消息通知中心（公告/待办/消息/预警列表 + 详情弹窗：上一条/下一条/历史消息）；后端新增 `/portal/notice/list` 接口
- [x] 第一个真实模块：生产加工单（全栈，锭加工场景）
  - 建表：`manu_order`（主表）+ `manu_order_item`（工序分录）+ 演示种子数据
  - 表头字段（已定稿）：合同号*、锭号*、批号*（如：正常/加急）、生产车间*、预开工日、预完工日、销售订单号、客户编码、客户、测试程序*、生产订单客户
  - 后端：分页查询（单号/锭号/状态/工厂筛选）、详情（含工序分录）、保存（自动单号 MO+日期+序号）、删除、审核/弃审/关闭（状态流转 0草稿→1已审核→2生产中→3已完工→4已关闭）；必填校验；补充 MyBatis-Plus 分页插件配置与全局异常处理
  - 前端：列表页（查询区/工具栏/双击编辑/分页）+ T+ 单据风格表单页（表头字段区 + 工序明细分录行内编辑 + 底部合计与审核信息），非草稿状态只读；mock 与真实接口双模式
  - 路由 `/prod/manufacture/order`（列表）、`/prod/manufacture/orderForm`（表单，支持 `?new=1` 快捷新增与桌面快捷入口）
- [x] 自研元数据表单引擎（类 PanelX，不依赖外部平台——已定稿方向）
  - 思路：表单字段/表格列/按钮/状态选项/明细定义全部 JSON 化存 `panel_config` 表，表单数据动态 JSON 存 `form_data` 表（不按面板建表）
  - 后端 `/api/px/*`：`getPanelConfig`、`getPermMatrix`、`getNewFormPermMatrix`、`getFormDescriptor`（data+meta+privilege 三合一）、`queryFormDataList`（关键字分页）、`callButton`（新增流程/提交/删除/审核/弃审/关闭 状态机）、`deleteForms`；`PxService` 统一处理
  - 前端 `src/panelx/engine.js`：对齐 PanelX SDK 调用风格（双模式：mock 默认 / `VITE_MOCK=false` 走真实接口）；`PanelxList.vue`（配置驱动列表）+ `PanelxForm.vue`（配置驱动表单，支持明细分录增删行、状态感知按钮、必填校验、草稿只读控制）
  - 生产加工单面板 `MANU_ORDER`（v3.0，2026-08-14 按实测 T+ 轻MES 一比一复刻）：
    - 表头 23 字段 3 列布局：合同号*(日期型)/**锭号*(=单据编号自动 MO-yyyy-MM-####)**/批号*/生产车间*/预开工日/预完工日/销售订单号/客户编码/客户/测试程序*/生产订单客户/机构*/重量/开工日期/完工日期/启用派工/自动转移/产品自动添加到材料/是否手工修改单据编码/外部单据号/负责人/启用领料申请/对方仓库；表尾 制单人/审核人/审核时间/打印次数/创建/修改时间
    - 表体三明细，各带「明细/汇总」子页签：产成品明细 19 列（生产类型/适用BOM/BOM展开方式/生产单位/齐套数量(主)/累计汇报套数/可用量/现存量/图号/单重/总重/需求令号）；材料明细 24 列（子件BOM/材料倒冲方式/允许循环/行中止/定额需用数量2/损耗数量手工录入/损耗率%隐藏）；工序明细 40+ 列（工艺类型/工艺序号/工作中心/设备/委外单价税率金额/按辅单位计价/工序完工状态/手工完工/行中止/工废·料废工价(辅单位)/工资类型/工价/金额=计划数量×工价/关键工序/单位标准工时/待返修数量-本序·他序发现/计划·完成时间）+ **工序材料子表**（展开行：材料编码/名称/规格/计量单位/需用/损耗）
    - 工具栏 T+ 分组：新增/选单/保存(保存新增·保存为草稿·保存为常用单据·保存打印)/删除/审核(弃审)/生单11项/变更/工具(现存量查询·变更历史·联查9项)/设置(单据设置·移动控件·调整宽度·工具栏设置)/打印(8项)/更多(复制·放弃·草稿·中止执行·重取BOM材料·附件·刷新·消息)；未实现项点击提示"演示环境暂未实现"
    - 列表页：11 字段查询区（合同号/锭号/批号/生产车间/预开工日/预完工日/销售订单号/客户编码/客户/测试程序/生产订单客户）+ 网格双视图（产成品明细/产成品明细汇总，按明细行展平+合计行）
    - 状态机：草稿→审核→已审核⇄弃审；中止执行(→已中止)/草稿(取消中止→已审核)；生产中/已完工由工序汇报驱动（演示数据预置）
    - 引擎扩展：queryFields/gridTabs/buttonGroups/subTabs/summaryItems/subTable/calc 表达式链/hidden 列；后端 PxService 同步（列表展平+条件过滤+buttonGroups+锭号自动单号）
  - MANU_ORDER 面板 v3.1（2026-08-14 二次实测 T+ 后补齐差距）：
    - 列补齐：产成品明细 21 列（+存货图片/产品字符公用自定义项1）、材料明细 24 列可见（+存货图片）、工序明细 44 列（+工序字符专用自定义项1）、工序材料子表 7 列（+计划数量）；列表网格同步 21 列；图片列渲染占位（el-image）
    - 工具栏补齐：新增「修改」组（已审核/生产中/已完工可用）、审核组 +审批情况、更多组 +取消中止（与"草稿"同规则恢复已中止单据）；快捷键提示（保存 Alt+S / 保存新增 Alt+\ / 保存打印 Alt+G / 直接打印 Alt+P / 打印 Alt+; / 预览 Alt+/ / 打印模板设置 Alt+, / 导出 Alt+X / 放弃 Alt+Z）
    - 表头补齐：第二个「测试程序」（非必填文本，displayName 机制支持同显示名不同键）
    - 表尾补齐：制单人/审核人/审核日期/审核时间/打印次数/创建时间/修改时间/变更人/变更日期/审核机器人（10 项，mock 数据带审核/变更字段）
    - 列表右键菜单（对齐 T+ 产成品明细）：定位/复制到剪贴板（Tab 分隔，clipboard API + execCommand 兜底）/从剪贴板粘贴/另存为EXCEL模板（导出 CSV）/批量修改/销售订单查询/存货中心/更多（未实现项提示"演示环境暂未实现"）
    - 前端 engine.js 与后端 init.sql panel_config/form_data 种子数据同步（Node 脚本改造，products 21/materials 25/processes 44/subTable 7/gridTabs 21/审核(3)/更多(9)）；E2E 验证通过（无头 Edge 9223 + localhost:5173）
  - 后续新单据：只需在 `panel_config` 加一条配置（或 mock 里加一份），页面代码零改动
- [x] 第二个真实单据：销售订单（全栈，2026-08-14 实测 T+ SA03 一比一复刻）+ 与生产加工单双向导通
  - 实测抓取：T+ 销售订单入口 智能供应链→销售管理→单据→销售订单（code=#SA03，URL /tplus/BAPView/Voucher.aspx?sysId=SA&mId=SA03&pId=voucherView）
  - 真实结构：工具栏 新增/选单/保存/删除/审核/生单（**含"生成生产加工单"**、销售出库/销售退货/转产成品入库/协同对方订单）/变更/工具（联查：出库情况/生产加工情况/订单执行图/退货情况/联查合同/生单流程联查）/设置/打印/更多（草稿 Alt+B/导入/整单中止）/智能（单据分享）；表头 2行5列 10 字段（*单据编号/*单据日期/*客户/*结算客户/部门/部门.负责人/业务员/项目/预计交货日期/联系人）+ 40+ 隐藏字段；表体单明细 15 列（明细/汇总子页签）；表尾 制单人/审核人/审核日期
  - SO_ORDER 面板（v1.0）：表头 11 字段（含客户编码下拉、单据编号自动 SO-yyyy-MM-####）、明细 15 列（含税单价=单价×(1+税率%)、金额=数量×单价、含税金额=数量×含税单价 计算链）、工具栏 12 组 T+ 分组、查询区 6 字段、明细/汇总子页签、表尾 10 项；引擎多面板化（panelOf 注册表：每面板独立 config+rows，getPanelConfig/getNewFormPermMatrix/getFormDescriptor/queryFormDataList/callButton/deleteForms 全部按 panelCode 分发）
  - 拉式选单（生产加工单→销售订单，对齐 T+ 选单前提：已生效且未中止）：工具栏「选单」→ 对话框列出已审核 SO（按单据分组）→ 多选 → 字段映射带入：单据编号→销售订单号、单据日期→合同号、客户/客户编码→客户/客户编码、结算客户→生产订单客户、业务员→负责人、预计交货日期→预完工日；明细行→产成品明细（存货编码→产品编码、存货名称→产品名称、规格型号、销售单位→生产单位、数量、现存量、需求令号=SO单号）
  - 推式生单（销售订单→生产加工单，对齐 T+「生单-生成生产加工单」）：SO 工具栏「生单」→ 生成独立 MO 草稿（自动单号、字段映射同上）→ 跳转新单表单；mock（createMoFromSo）与后端 PxService（case "生成生产加工单" + generateFormNo 按面板前缀 MO-/SO-）双实现
  - 页面概念（定稿）：每张单据 = 一个独立面板 = panel_config 独立配置 + form_data 独立数据 + 独立路由（/panelx/list|form/:panelCode）+ 独立单号前缀（MO-/SO-）；单据间通过选单/生单关联，字段映射集中管理；后续新单据只需加 panel_config 配置与种子数据，页面代码零改动
  - E2E 验证（无头 Edge 9223）：SO 列表/表单渲染、拉式选单带入（销售订单号/合同号/客户/客户编码 KH003/预完工日/2 行产品明细）、推式生单跳转 MO-2026-08-14-0001（销售订单号/合同号 2026-08-12/客户 中天精工/预完工日 2026-08-15/CP002+CP004 明细）全部通过；后端 mvn compile BUILD SUCCESS
  - [x] 第三个模块：基础设置全部面板复刻（2026-08-14，35 个面板按实测 T+ 一比一复刻）
    - 实测抓取：T+ 基础设置入口 code=#AA1001 部门/#AA1022 存货/#AA1041 物料清单/#SYS0404 选项设置/#SYS0911 期初余额 等；菜单数据接口 LoadPortalData（Ufida.T.EAP.Portal.UIP.ashx?method=LoadPortalData）提取全部 36 项真实 URL；页面类型：BaseInfoList（基础档案）/OpenList（关联设置）/DoubleList（双列表）/CommonList（通用查询）/Voucher（单据）/v2 设置页（选项设置/看板授权/打印中心/大屏等）
    - 基础档案 13：部门 DEPT（4 列）/员工 EMP（11 列）/往来单位 PARTNER（15 列）/计量单位 UOM/存货 INV（12 列）/设备 EQUIP/班组 TEAM/工作中心 WC（15 列）/工序 OP（13 列）/仓库 WH（7 列）/地区 REGION/项目 PROJ/不合格原因 REJECT；种子联动现有系统（存货 CP001-005/CL001-005、工序 PX001-007、班组/工作中心/设备/仓库/客户 KH001-005 与 MO/SO/库存一致）
    - 关联设置 3 + 价格本：往来单位存货设置 PARTNER_INV（14 列）/工序辅单位换算率设置 OP_CONV（10 列）/存货价格本 INV_PRICE（25 列：采购/委外/零售/普通客户/一~五级批发价 价格体系）
    - 双列表 2：工艺路线 ROUTE（表头+工序明细：加工顺序/工序/工作中心/设备/班组/工时/工价，审核流）、物料清单 BOM（父件表头+子件明细：子件/定额数量/损耗率/需用数量=定额×生产数量 计算链）；查询 2：物料清单正向查询 BOM_FWD（16 列）/反向查询 BOM_REV（9 列）
    - 系统设置 12：选项设置 SYS_OPT（设置表单：小数位/负库存/自动审核/编码前缀）、看板授权 SYS_BOARD_AUTH、单据设计 SYS_BILL_DESIGN（面板列表，设计器开发中）、移动单据设置 SYS_MOBILE、移动模板设置 SYS_MOBILE_TPL、单据编码设置 SYS_CODE（编码规则：前缀/日期格式/流水号/当前值）、打印管理中心 SYS_PRINT、打印出厂值设置 SYS_PRINT_DEFAULT、预警设置 SYS_ALARM、任务管理 SYS_TASK、大屏设备管理 SYS_SCREEN、大屏客户端下载中心 SYS_SCREEN_DL
    - 初始化 3：库存期初余额 INIT_BALANCE（表头 仓库/日期 + 明细 8 列：数量/主单价/金额）、期初暂估入库单 INIT_AP（明细 7 列：实收数量/单价/金额）、期初销售出库单 INIT_AR（明细 5 列）
    - 菜单：menus.js 基础设置组全部指向 /panelx/list/<CODE>（三级悬浮卡片形态，对齐 T+ 左侧导航）；engine.js BASE_CONFIGS 注册表（panelOf/nextNoFor/flattenFor 扩展）+ Object.assign 合并 SYS/INIT；档案工具栏 T+ 分组（新增/修改/删除/查找/打印/导入/更多），列表"修改"打开编辑
    - 后端：init.sql 追加 35 面板 panel_config + 130 条 form_data 种子（Node 生成器 + vm 提取配置，幂等 INSERT）；E2E 验证通过（DEPT 4 列 10 行/INV 12 列/BOM 父件+子件明细/ROUTE 工序明细 3 行/INIT_BALANCE 明细/菜单挂载），截图 docs/ref/mes-live/30-35
    - 踩坑记录：T+ 门户 menu-sub 折叠态三级菜单 dispatch click 会被错误路由（打开库存单据）——需真实 hover/或直接构造 URL；menuTree 数组双逗号（}, ,）产生 null 元素导致 LeftNav 渲染崩溃（reading 'code'）——菜单块拼接时注意尾逗号
  - 遗留：自研 OrderList/OrderForm 弃用（文件保留作参考）
- [x] **PanelX 平台集成**（2026-08-14，方案 A：浏览器 SDK 直连 demo.kwaidoo.com，详见第十章）
- [x] T+ 门户保真改造（2026-08-13 抓取 h2t.chanjet.com 真实 HTML/CSS 对照实施）
  - 设计 token 化（`src/styles/index.css`）：主蓝 `#289be5`、激活页签紫 `#663ECF`、角标红 `#fc5c5e`、顶栏 `#f0f0f0`、侧栏 `#fafafa`、内容区 `#f9f9f9`、文字三级 `#343a42/#76808f/#a7adb6`；Element Plus `--el-color-primary` 系列同步对齐；暗色模式 token 一并覆盖
  - TopBar 三段式（`TopBar.vue`）：左 Logo+分隔线+工厂切换（带刷新）；中 账号/认证状态/登录日期/服务到期；右 内嵌搜索框（下拉直达菜单）、更新公告（new 角标+详情）、移动端、待办/消息/预警、全屏、帮助下拉（帮助文档/AI 智能帮助/新手引导/关于）、用户下拉（账号管理/修改密码/界面设置/换肤/工作台设置/初始化向导/退出）
  - TabsBar 右侧按钮组（`TabsBar.vue`）：快速查找单据（按编号直达 `/panelx/form/MANU_ORDER?id=`）、最大化/恢复（隐藏侧栏）、关闭全部、更多下拉；激活页签改 3px 紫下划线，高 32px
  - NoticeCenter 调整为待办/消息/预警三类（公告移入更新公告入口），角标用 T+ 红
  - 右侧帮助面板 `HelpPanel.vue`（动态/消息/知识库/帮助教程四 tab + 常用操作直达）
  - MES 初始化向导 `InitWizard.vue`（三步：行业细分&经营业态 → 启用模块&报工方式 → 完成匹配专属桌面菜单；首次登录自动弹，可从用户下拉重开）
  - `stores/app.js` 新增 maxContent/helpVisible/helpTab/initWizardVisible/initDone 状态；`stores/user.js` 新增登录日期与服务到期

## 五、运行方式

**前端**（默认 mock，无需后端）：
```bash
cd frontend && npm run dev   # http://localhost:5173
```
演示账号：`admin / 123456`（mock 模式任意账号可登录）

**后端**：
1. 初始化数据库：`mysql -uroot -p < backend/src/main/resources/db/init.sql`
2. 修改 `backend/src/main/resources/application.yml` 的数据库账号密码
3. `backend/build.bat` 打包 → `backend/run.bat` 启动（http://localhost:8080）（两脚本自动探测 JDK，无需配置）

**联调**：`frontend/.env.local` 写入 `VITE_MOCK=false`

## 六、待办（下一步）

- [ ] 生产加工单前后端连库联调（需本机 MySQL 初始化 init.sql 后验证真实接口）
- [ ] 单据设计器页面：可视化编辑 panel_config（字段/列/按钮/明细），对接 `savePanelConfig`
- [ ] 权限矩阵按角色生效（当前 admin 全量可见可写）
- [x] 工序汇报单面板（2026-08-14 已按真实 MR10 一比一复刻，见顶部条目）；「审核后联动 MANU_ORDER 工序完工状态」待真实接口验证
- [ ] init.sql panel_config 补齐（当前仅 MANU_ORDER/SO_ORDER/SALE_OUT/FINISH_IN/PROCESS_REPORT，基础档案 35 面板与库存其余 4 单的 panel_config 缺失，后端模式需从 engine.js 生成器统一产出）
- [ ] 待办/消息/预警角标接真实数据
- [ ] 剩余面板字段 options 改造为引用基础数据（dataType: 参照 + refPanel + refMap 带出，见十一-1 违反清单：MANU 产品编码/对方仓库/工作中心/班组、SO 部门/业务员、库存 6 单 仓库/存货、BASE_CONFIGS 引用单据名 等；确无对应档案的字段保留下拉并注释说明）
- [x] 补齐 T+ 对照差距：单据查询/新增单据快捷入口（左菜单顶部）、工作台设置、消息通知弹窗（含界面设置）

## 七、已知事项

- 前端默认 mock 数据模式；生产加工单已按双模式开发，`VITE_MOCK=false` 切真实接口（需后端+MySQL）
- 后端 jar 已编译通过但尚未在本机连库运行；MyBatis-Plus 3.5.9 分页需 `mybatis-plus-jsqlparser` 依赖
- Lombok 在 JDK 26 下不可用，已全部移除改用手写 getter/setter
- 演示账号密码种子为 BCrypt：`$2b$10$GMsKtu.Ws4ih9nJLSa00xOK6MKCrvyS0XnIK3B0qR0i/qFBiBKsk6`（明文 123456）
- 后端 JWT 密钥在 application.yml，生产环境需更换

## 八、参考对象（畅捷通 T+ 门户）结构分析

> 2026-08-13 抓取自 `https://h2t.chanjet.com/tplus/view/portal/portal.html`（未登录外壳）

### 生产加工单面板设计（T+ 官方帮助文档，已抓取存 docs/ref）

- 官方文档：http://t.static.chanjet.com/helpfiles/Cloud/StdPro/WebHelp/Content/MP/ManufactureOrder.htm（全文存 `docs/design/T+生产加工单-官方帮助文档.txt`，界面截图存 `docs/ref/tplus-screenshots/tplus-ref-*.jpg|png`）
- 表单结构 = 表头 + 表体 4 页签（产成品明细/材料明细/工序明细/汇总）+ 表尾 + 盖章状态（已审/中止）
- 操作 = 新增（手工填单 / 选单：销售订单、预测单）、生单（推式生成材料出库单、产成品入库单）、采购需求分析、中止/取消中止、手工完工/取消完工、联查、现存量查询、转换（请购单/采购订单/调拨单）
- 关键业务规则：BOM 展开（单阶/尾阶）、定额需用数量计算（需用=定额需用×主数量/定额生产数量；计划=需用+损耗）、倒冲料（产成品入库自动生成材料出库）、领料工序、工序行码（扫码报工）、末工序标记、累计合格/不合格数量、工价/金额（主数量×工价）

### 生产加工单真实面板实测（2026-08-14 进 T+ 体验站轻MES 账套抓取）

- 进入路径：t.chanjet.com/tplus/view/login.html → 立即体验 → 选领域「新制造」→ 行业「轻MES」→ 自动分配演示账号 tplusdemo12852（无头 Edge + puppeteer 自动化实测）
- 真实单据页：`/tplus/BAPView/Voucher.aspx?sysId=mp&mId=mp05&pId=voucherView`（表单为同页 voucherStateControl=New）
- 完整实测设计文档：`docs/design/T+轻MES生产加工单-真实面板设计.md`；截图 `docs/ref/tplus-live/`（登录页/体验弹窗/门户/加工单列表+表单）
- 关键实测结论：
  - 真实锭号 = **单据编号**（自动 MO-yyyy-MM-####），合同号为日期型；批号/生产车间/测试程序为必填参照
  - 表头 28+ 字段（3列×10行：机构*/重量/开工/完工/启用派工/自动转移/产品自动添加到材料/手工修改编码/外部单据号/负责人/启用领料申请/对方仓库 + 表尾制单人/审核人/审核日期/审核时间/打印次数/修改人/变更人/变更日期）
  - 表体 = **三明细**（multidetailsudi：产成品明细/材料明细/工序明细），每明细有「明细/汇总」双子页签
  - 产成品明细 21 列；材料明细 24 列（材料倒冲方式/允许循环/行中止/定额需用数量2）；工序明细 43 列（工废·料废工价/工资类型/关键工序/单位标准工时/待返修数量-本序·他序发现/计划·完成时间 + 工序材料子表）
  - 工具栏真实分组：新增/选单/保存(新增·草稿·打印)/删除/审核/**生单11项**/变更/工具(现存量查询·变更历史·联查9项)/设置(单据设置·移动控件·调整宽度·工具栏设置)/打印(8项含明细标签·工序流转卡)/更多(放弃·草稿·中止执行·重取BOM材料·附件)
- 待办：~~按实测文档将 MANU_ORDER 面板二次对齐~~（已完成：2026-08-14 v3.0 一比一复刻，含 E2E 实测：材料计算链 需用=2×200÷1=400、保存跳转、工序子表、三明细子页签全部验证通过，截图 `docs/ref/mes-live/`）

### 智能供应链流程结构实测（2026-08-14 复用既有门户会话抓取）

- 进入方式：无头 Edge + CDP 9222 复用已登录门户 `https://h2t.chanjet.com/tplus/view/portal/portal.html?_from=exp`（账套 [012007]，演示账号 tplusdemo12855）
- 菜单 DOM：一级 `li.menufist_ISC[data-code=ISC]` → 二级 `li.newML[data-code]`（SA/ST）→ `div.menu-sub` 内 `dt.secMenu` 分组 + `dt.thrMenu` 项（`a[code]` + `alltitle`）
- 实测结构（含菜单码）：销售管理 SA（单据：销售订单 #SA03 / 统计表 #SA6001 / 明细表 #SA4001；执行表：销售订单执行表 #SA8006；更多：销售订单生产进度表 #SA8018）；库存核算 ST（单据：采购入库单 #ST1001 / 产成品入库单 #ST1002 / 其他入库单 #ST1004 / 销售出库单 #ST1021 / 材料出库单 #ST1022 / 其他出库单 #ST1024，各带统计表/明细表；成本核算：成本手工维护 #ST3030；库存账表：库存状况表 #ST9001 / 收发存汇总表 #ST6061 / 库存台账 #ST9002）
- 存档：`docs/ref/tplus-live/智能供应链-流程结构.{md,json}` + 截图 `智能供应链-菜单.png`

### 库存核算 6 单据真实面板实测与一比一复刻（2026-08-14）

- 进入方式：无头 Edge + CDP 9222 门户会话（演示账号 tplusdemo12853）→ 门户内点库存核算菜单 → voucher iframe（`BAPView/Voucher.aspx?sysId=ST&mId=XXXX&pId=voucherView`，列表/表单同页，点新增切换）
- 抓取脚本：`%TEMP%\pptr\grab-inv4.cjs`（门户点击菜单 → 等 iframe → 抓列表+新增表单结构）；原始数据存档 `docs/ref/tplus-live/库存核算-20260814/`（每单 report.json + body 文本 + 截图），汇总 `inv-summary.md`
- 实测共性：网格页签固定 **明细+汇总**（tab_RDRecordDetails/_Sum）；右键 定位/复制到剪贴板/…/现存量提取/更多；表尾 备注/制单人/代理人/修改人/审核机器人/审核人/审核日期/审核时间/打印次数/…
- 实测结构（表头/明细列/工具栏差异）：
  - **ST1001 采购入库单**：表头 16（*单据日期/*单据编号/*业务类型/入库类别/供应商编码/*供应商/*供应商简称/匹配来源单号/经手人/验货人/项目/仓库/来源单据/外部单据号/来源单号/销售订单号）；明细 22 列（*仓库/*存货名称/存货图片/规格型号/*实收数量/*计量单位/实收数量2/计量单位2/计量单位组合/换算率/单价/税率%/单价2/含税单价2/含税单价/金额/含税金额/费用调整/费用金额/现存量/现存量说明/产成品图片）；工具栏差异：选单-智能选单、转换-转成材料出库单、协同、工具-入库调整情况、更多-重新取价
  - **ST1002 产成品入库单**：表头 8；明细 12 列（*产品名称/*仓库/存货图片/规格型号/智能选单/*计量单位/金额/单价/*实收数量/现存量/现存量说明/图号）；选单-选产成品入库单（自制加工）/选生产加工单、生单-生成产成品入库单（自制退库）等
  - **ST1004 其他入库单**：表头 7；明细 12 列（*仓库/*存货名称/规格型号/*计量单位/*数量/智能选单/计量单位2/数量2/单价/金额/现存量/现存量说明）；选单-其他出库单、转换-转换成其他出库单
  - **ST1021 销售出库单**：表头 9（含*客户/*结算客户）；明细 19 列（*仓库/*存货名称/*存货编码/规格型号/*计量单位/*数量/智能选单/成本价/税率%/售价/含税售价/销售金额/税额/含税销售金额/折扣金额/现存量/现存量说明/需求令号/退货原因）；选单-选销售订单、生单-生成销售出库单(销售退货)
  - **ST1022 材料出库单**：表头 10（含出库类别/生产车间/领用人）；明细 12 列（*仓库/加工单号/*材料名称/*计量单位/*数量/单价/金额/规格型号/手工确定成本/明细备注/现存量/现存量说明）；选单-选材料出库单（直接领料）/（自制领料）/选生产加工单、选单转换、生单-退料
  - **ST1024 其他出库单**：表头 5；明细 9 列（*仓库/*存货名称/规格型号/*计量单位/*数量/单价/金额/现存量/现存量说明）；转换-转换成其他入库单、工具-联查设备投放单
- 编号规则实测：RK/CP/IC-2026-08-####（入库）、IO/MD/ID-2026-08-####（出库）
- 复刻实现（engine.js INV_CONFIGS + 演示数据 INV_SEED）：6 面板配置化（invPanel 工厂），查询区/明细+汇总双子页签/工具栏分组与真实一致；menus.js 挂 panelCode（`/panelx/list/PURCHASE_IN` 等）；router 排除 panelCode 菜单走 PanelxList；PanelxList/Form 去硬编码（汇总按存货名称分组、标题取单据编号）
- 验证：无头浏览器 6 面板列表+表单全过（表头字段数与真实逐一相同），截图 `%TEMP%\pptr\inv-verify\`

### 库存核算报表面板实测（2026-08-14）

- 进入方式：门户点击库存核算菜单 → 报表页（`/tplus/v2/bap/report?ReportName=XXXX`，先弹查询条件对话框 → 点「查 询」进报表主体）；成本手工维护为列表页（`BAPView/OpenList.aspx?mId=ST3030`）
- 报表工具栏（统一）：查 询 / 打印 / 导出 / 发送邮件 / 刷新 / 退出；表尾合计/小计/共 N 条/每页条数/跳至页
- 抓取产物：`docs/ref/tplus-live/库存核算报表-20260814/`（每表 01-query/04-body/03-structure + 截图），汇总 `rpt-summary.md`
- 实测列结构（明细表/统计表/账表）：
  - **明细表 ×6**（ST4001/4002/4004/4005/4006/4008）：30/23/23/29/28/27 列。采购入库明细表 30 列：单据日期/创建时间/单据编号/业务类型/仓库编码/仓库/入库类别/供应商编码/供应商/部门编码/部门/经手人编码/经手人/备注/制单人/审核人/存货编码/存货/规格型号/计量单位/实收数量/单价/金额/单价2/计量单位2/实收数量2/入库调整/费用调整/总成本/费用金额；其余明细表为对应单据字段（销售出库多应发数量/成本价/成本金额/销售订单号/入库单号，材料出库多领用人/工作中心/班组/工人/设备，其他出入库多出库调整/累计调拨入库量/合理损耗数量）
  - **统计表 ×6**（ST6001/6002/6004/6011/6012/6014）：17/12/12/12/13/14 列，按 仓库+存货 聚合：仓库编码/仓库/存货编码/存货/规格型号/主单位/辅单位/数量(主单位)/单价(主单位)/金额/数量(辅单位)/单价(辅单位)/入库调整/费用调整/总成本/费用金额（销售出库按周统计单据日期（周））
  - **成本手工维护 ST3030**（OpenList 列表）：工具栏 查找/更新单据/设置/批量取成本/汇总维度设置/汇总导出/汇总导入/模板下载/退出；明细/汇总页签；列：单据类型/单据编号/单据日期/仓库/存货编码/存货分类/存货/规格型号/BOM版本号/计量单位/数量/*成本价/*成本金额/制单人
  - **库存状况表 ST9001**：9 列（仓库编码/仓库/存货编码/存货/规格型号/主计量/现存量(主)/结存单价(主)/结存金额）；仓库多选（备件库/原料仓/成品仓…）
  - **收发存汇总表 ST6061**：17 列（仓库编码/仓库/存货编码/存货/规格型号/主单位/辅单位 + 期初结存/本期入库/本期出库/期末结存 × 数量(主单位)/平均单价/金额 + 数量(辅单位)/计量单位组合）
  - **库存台账 ST9002**：13 列（单据日期/单据类型/单据编号/业务类型/往来单位/项目/收入/发出/结存 × 数量(主单位)/单价/金额；含期初结存行）
- 查询条件（以采购入库明细表为例）：单据日期/业务类型/入库类别/供应商/存货分类/单据状态/单据编号/仓库/供应商分类/供应商名称/存货（报表查询框统一：公共方案/系统方案/个人方案 + 查询条件分组设置 + 添加过滤条件 + 不显示查询条件直接进入报表）

### 面板交互设计定稿（点击填写 / 弹窗 / 选单 配置驱动化）

- 规范文档：`docs/design/面板交互设计规范.md`（后续所有设计面板沿用）
- 核心改动（engine.js + PanelxList.vue + PanelxForm.vue）：
  - **拉式选单配置驱动**：面板配置新增 `selectConfig`（source 来源面板 + title/tip/columns 弹窗定义 + headerMap/detailMap 字段映射）；MANU_ORDER/SO_ORDER/SALE_OUT/MATERIAL_OUT/FINISH_IN 已配置
  - PanelxForm.vue：选单弹窗按 selectConfig 动态渲染列；确认后按 headerMap 带表头、detailMap 带明细、回填来源单号；不再硬编码 MANU_ORDER
  - PanelxList.vue：选单入口按 `cfgCache.selectConfig` 存在与否跳转/提示
  - engine.js：getNewFormPermMatrix / getFormDescriptor 返回 selectConfig
- 验证（无头浏览器）：SALE_OUT「选单-选销售订单」弹窗标题/列来自配置，确认后客户/结算客户=西部材料、明细 6 字段映射带入（存货名称/编码/规格/计量单位/数量/现存量）；MANU_ORDER 选单回归正常

### 顶栏 navbar
- 左：畅捷通 Logo + T+ Logo
- 中：用户账号、企业名称、企业认证状态、登录日期、服务到期时间
- 右：全局搜索（搜索-产品功能）、更新公告、企业认证、移动端、待办/消息/预警（角标 + popover）、服宝、帮助（帮助文档/新手引导/关于）、用户下拉（账号管理/界面设置/换肤/工作台设置/回到旧版/退出）

### 左侧菜单
- 顶部快捷区 func-zone：展开菜单、单据查询、新增单据
- 双模式并存：平铺（sidebarMenu）+ 手风琴（accordionmenu），可切换

### 主内容区
- 页签条 + 右侧按钮：快速查找单据、最大化/恢复、关闭全部页签、更多页签下拉
- 每个页签一个 iframe 装载内容

### 浮层
- 消息通知弹窗（上一条/下一条/历史消息）
- 右侧帮助面板（动态/消息/知识库/帮助教程）
- 行业化配置向导（三步）

### 与轻MES 对照
| T+ 特征 | 轻MES 现状 |
|---|---|
| 顶栏三段式（logo+账套切换/账号·认证·登录日期·到期/搜索·公告·角标·帮助·用户） | 已覆盖（2026-08-13 保真改造） |
| 内嵌全局搜索"搜索-产品功能" | 已覆盖（顶栏内嵌输入框+下拉直达） |
| 更新公告（new 角标）+ 消息通知弹窗 | 已覆盖（公告 popover+详情；通知中心上一条/下一条/历史消息） |
| 平铺/手风琴双菜单模式 + func-zone 快捷区 | 已覆盖 |
| 多页签 + 右侧按钮组（快速查找/最大化/关闭全部/更多） | 已覆盖（keep-alive 替代 iframe；快速查找按单号直达加工单） |
| 激活页签紫色下划线 #663ECF | 已覆盖 |
| 右侧帮助面板（动态/消息/知识库/帮助教程） | 已覆盖（HelpPanel 抽屉） |
| 行业化配置向导（三步） | 已覆盖（MES 初始化向导，首次登录自动弹出） |
| 企业名称/账套切换 | 工厂切换替代 |
| 工作台设置/界面设置 | 已实现（用户下拉入口） |
| 服宝机器人/AI 智能帮助 | AI 智能帮助入口指向帮助面板知识库（占位） |
| 移动端 | 顶栏移动端入口（扫码报工占位） |

## 九、开发掌握要点

### 1. 进入真实 T+ 轻MES 演示环境（最重要，后续每改一个模块前先看真实的）

**人工进入（3 步）：**
1. 打开 https://t.chanjet.com/tplus/view/login.html
2. 点「立即体验」按钮 → 弹窗选领域「新制造」→ 行业「**轻MES**」
3. 系统自动分配演示账号并登录进账套，无需注册

**自动化进入（无头 Edge + puppeteer-core，实测可行）：**
- 启动：`msedge --headless=new --remote-debugging-port=9222 --user-data-dir=%TEMP%\edge-tplus-profile`
- 步骤：连接 9222 → 打开登录页 → 点 `#expBtn` → 等 iframe（url 含 selectRoles）→ 在 iframe 内点 `#mes`（轻MES 行业项）→ 自动经 ossjump 落 h2t.chanjet.com 门户
- 脚本在 `%TEMP%\pptr\go.cjs`（注意：demo 账号接口有 WAF 校验，纯 HTTP 请求必返回 success:false，必须真实浏览器上下文走完整点击流程）

**底层流程（源码逆向所得）：**
1. 立即体验 → 弹层 `/tplus/view/selectRoles.html`
2. 弹层请求 `GET https://t.chanjet.com/api/login/demoaccount/config` 返回领域/行业树；轻MES = 领域 `newmanufacturing` + 行业 `mes`
3. 点行业项 → `GET /api/login/demouser?industryCode=newmanufacturing&groupCode=mes` → `{success,userName,pwd,accNum,sid,domain}`
4. 跳转 `https://{domain}/tplus/view/ossjump.html?fromApp=exp&sid=..&user=..&pwd=..&accNum=..` 自动登录

**登录后的关键事实：**
- 演示账号：tplusdemo12852（系统每次分配可能不同）；账套域：h2t.chanjet.com
- 生产加工单页：`/tplus/BAPView/Voucher.aspx?sysId=mp&mId=mp05&pId=voucherView`；菜单项 `a.thrMenuFirst[code="#MP05"]`（在活动页面派发 click 比拼 URL 更稳）
- 列表与表单同页：点新增后 `voucherStateControl=New`，表体渲染在页面下半部
- 真实语义：**锭号 = 单据编号**（自动 MO-yyyy-MM-####），合同号为日期型

### 2. 真实面板设计抓取技巧（工序汇报单/返修工作台/看板等模块复用）

- 工具栏分组清单：iframe 内 `document.body.innerText` 前 ~100 行即完整工具栏
- 表格列定义：HTML 里 `<col t="列名" a="字段编码" flag="myhidden">`，按序过滤 hidden 即列结构
- 表头字段布局：`div.vch-ctldiv` 带 `left/top` 坐标 + `<label>*字段名</label>`，按 top 分组即 3 列行布局
- 表体明细定义：容器属性 `multidetailsudi="137188产成品明细137191材料明细137390工序明细"` = 明细分录清单
- 页签成对出现：`tab_XXX`（明细）+ `tab_XXX_Sum`（汇总）
- 抓取产物一律存档：`docs/ref/tplus-live/`（截图+DOM 文本）、`docs/design/T+轻MES生产加工单-真实面板设计.md`

### 3. 本地应用无头验证备忘

- 无头 Edge：`--remote-debugging-port=9223 --user-data-dir=%TEMP%\edge-mes-profile`；puppeteer-core 装在 `%TEMP%\pptr\node_modules`
- 登录态注入：localStorage 写 mes_token/mes_user 后**必须整页 reload**（Pinia 状态驻内存，不刷新守卫不认）
- 点工具栏按钮要选 `.tb-main` 本体，不能点 `.tb-group` 容器（其 textContent 含下拉子项文本，点容器不触发）
- 验证计算链等表格值：按表头名定位单元格（`td[headIndex]` 内 input），避免 inputs 顺序与列错位
- ElMessage 3 秒自动消失，验证要 300ms 轮询抓取
- 写工具新建文件会 EISDIR：先 `New-Item` 建空文件再 write 覆盖
- 本机日期 2026-08-13，锭号/单号按本机日期生成（实测值以本机为准）

## 十、PanelX 平台集成（2026-08-14 · 方案 A 前端直连）

> 目标：轻MES 表单引擎对接 PanelX 低代码平台（`https://demo.kwaidoo.com/VF_DEV/`，业务域 `SdkTest`，账号 `admin/123456`）。

### 1. 方案选型（三选一，已定 A）

| 方案 | 说明 | 结论 |
|---|---|---|
| **A 前端直连 SDK** | 浏览器加载 `preload.js` → `window.PanelXSdk`，engine 非 mock 分支走 `sdk.api.*` | ✅ 已实施（CORS 全开：`Access-Control-Allow-Origin` 回显任意源 + 允许 `BusDomainCode` 头） |
| B 后端代理网关 | Spring Boot 转发 demo 网关，token 不落浏览器 | 工作量 3-5 倍，适合生产 |
| C 平台当低代码底座 | 在平台设计器重建 MES 面板，前端退化为渲染器 | 依赖生产环境设计器，远期 |

### 2. 改动清单（全部前端）

- `frontend/index.html`：`<head>` 加载 `<script src="/panelx/preload.js" devSdkUrl="https://demo.kwaidoo.com/VF_DEV/wp-core/api/getPanelXSdk">`（localhost 走 devSdkUrl；生产环境 preload 自动向上 ping 探测 baseURL）
- `src/api/index.js`：新增 `USE_PANELX`（`VITE_PANELX=true`）与 `USE_PORTAL_MOCK`（门户登录/菜单/角标在平台模式下仍走 mock，避免依赖本地后端）
- `src/panelx/engine.js`：新增平台适配层
  - `PANEL_MAP`：`MANU_ORDER → SdkTest_IML_00002` 面板码映射（`resolvePanelCode`）
  - `mapType`：平台 dataType（STRING/Decimal/下拉框('IML_00001_CM','文本');/Relate）→ 引擎中文类型
  - `adaptPanelConfig`：平台 getPanelConfig → 引擎配置（`tableColNames` 逗号串 → gridTabs 列、dataSchema → queryFields、topBarBtn → buttonGroups；平台面板无明细 → `detail:{tabs:[]}`）
  - `adaptMeta`：平台 meta 的 code 是拼音/英文（如 `shu1Ru41`），重写为中文名作绑定键（平台数据键即中文名）
  - `adaptFormData`：平台状态字段 `测试流程状态` → 视图的 `单据状态`（驱动只读/标签）
  - `platformConfig`：SDK 调用 + 适配缓存；`ensurePanelx()`：平台模式登录守卫
  - 7 个接口分支：`getPanelConfig/getPermMatrix/getNewFormPermMatrix/getFormDescriptor/queryFormDataList/callButton/deleteForms` 在 `USE_PANELX` 时走 `sdk.api.*`（按钮名原样透传，不再做本地后端的中止/取消映射）
- `src/views/panelx/PanelxList.vue` / `PanelxForm.vue`：`load()` 前先 `ensurePanelx()`，未登录弹出 `PanelxLogin`（复用既有组件）；新增成功后返回列表；表单「取消」= 返回

### 3. 实测结果（无头 Edge + puppeteer-core）

- ✅ MES 登录（门户 mock）→ 生产加工单列表 → 自动弹 PanelX 登录框 → `admin/123456` 登录成功（token 写入 `SdkTest_SdkTest_jwt_token`）
- ✅ 列表加载平台真实数据：面板「测试流程面板」、查询区 5 字段（关联用户/金额/输入1/输入2/关联）、网格 6 列、**18 条**数据（输入1/输入2/测试流程状态/金额/关联用户…）
- ✅ 表单页渲染平台字段（关联/金额/关联用户/输入2/输入1），`getNewFormPermMatrix`/`getFormDescriptor` 数据+meta+权限正常
- ⚠️ **「新增流程」按钮不落库（demo 后端限制）**：`callButton2` 无论 payload（中文键/拼音键/空/带 buttonParam/完整数据）均返回 `{data:{测试流程状态:发起},meta,privilege}`（表单矩阵）而非文档所述的 `{uuid,formModelId,data,extFields}`，且 `getFormFlowInfo` 报 SdkCommonException —— 该演示面板的规则是桩实现，非集成代码问题（参考文档「已知问题」也标注了 demo 模型 bug）
- 分页/查询（condition 精确匹配、keyword、advancedConditions）均验证可用

### 4. SDK 调用踩坑备忘（PanelX SDK v1.4.0）

- `sdk.api.getPanelConfig(panelCode)` **接收字符串**：传对象会被序列化进 query（`panelCode={"panelCode":...}`）导致 400「无法找到面板编号」
- `getPanelConfig` 返回 `{state,msg,data:{metadata,dataSchema}}` 包装，需取 `.data`（metadata/dataSchema 已由 SDK 自动 parse）；其余接口返回 data 本体，`unwrap()` 兼容
- 登录接口：`POST /wp-core/api/user/login`，头 `BusDomainCode`，body `{userName,password}`，JWT 30 天；storage 键 `SdkTest_SdkTest_jwt_token`（`${busDomainCode}_${appCode}_*`）
- CORS 预检允许 `BusDomainCode,Content-Type` 头，localhost 任意端口直连可行
- el-dialog 关闭后 DOM 残留（display:none），自动化断言不能以「元素消失」为准，应等业务信号（如表行出现）

### 5. 运行方式

```bash
cd frontend
$env:VITE_PANELX='true'; npm run dev   # PowerShell
# cmd: set VITE_PANELX=true && npm run dev
```

进入「生产加工单」菜单即走平台数据；PanelX 未登录时弹窗输入 `admin / 123456`。切回 mock：去掉 `VITE_PANELX` 即可。


## 十一、开发约束（定稿）

> 所有面板开发必须遵守以下约束。违反约束的代码属于技术债，需列入待办改造。

### 1. 字段引用基础数据约束（强制）

**规则：面板字段如果语义上对应基础档案里的数据，必须使用「引用字段」关联对应基础面板——能对应的字段必须对应上。仅当基础档案中不存在对应语义的档案时才允许手写静态 options 文本数组（且需在字段注释中说明原因）。**

**引用字段的录入交互（已实现，全部面板通用）：点击参照字段 → 弹窗（RefPickDialog）展示引用面板的数据列表（列 = `refColumns` 配置，缺省取引用面板网格列）→ 勾选行 → 确定导入：值写入 `refField`，`refMap` 把选中行的关联字段带出到本面板字段，`refMulti: true` 支持多选（值顿号连接，带出取第一行）。**

- 违反示例（禁止，基础档案有对应语义时）：`{ dataName: '生产车间', dataType: '下拉框', options: ['熔铸车间', '轧制车间', ...] }`
- 正确写法（引用字段）：

```js
{
  dataName: '客户',
  dataType: '参照',                   // 引用类型
  refPanel: 'PARTNER',                // 引用的基础档案面板编码（见下方登记表）
  refField: '往来单位名称',            // 存储值字段（写入本面板数据）
  displayField: '往来单位名称',        // 显示字段（界面展示）
  filter: { 停用: false },            // 可选：引用数据过滤条件（弹窗与下拉均生效）
  refMap: [                           // 可选：选中行 → 本面板字段 带出映射
    { from: '往来单位编码', to: '客户编码' },
    { from: '结算客户', to: '结算客户' },
  ],
  refColumns: ['往来单位编码', '往来单位名称', '结算客户', '分管部门', '分管人员', '停用'], // 可选：弹窗表格列（缺省取引用面板网格列）
  refMulti: false,                    // 可选：true 支持多选导入（值顿号连接）
}
```

- 引擎行为（mock 下）：弹窗/下拉数据由引擎从 `BASE_CONFIGS[refPanel].seed` 动态提取（filter 精确过滤 + 关键字模糊），**不写死在面板配置里**；真实接口/平台模式下走 `queryFormDataList` 查询引用面板
- [x] 销售订单明细存货参照化 + 多选勾选直接新增（2026-08-14，落地开发约束十一-1）
  - SO_ORDER 明细「存货名称」改为参照字段：refPanel INV / refField 存货名称 / displayField 存货名称 / refMulti 多选 / refMap（存货编码→存货编码、规格型号→规格型号、品牌→存货名称.品牌、计量单位→销售单位、**参考成本→单价**）
  - 参照弹窗 RefPickDialog（并行会话实现，engine 提供 normRef/refColumns/queryRefRows/refLabelOf/refPanelName）：从 INV 档案拉取列表（支持 filter 过滤 + 关键字），勾选多行 → 确定导入
  - **多选批量新增（本需求核心）**：onRefConfirm 明细分支——当前行填充第一个选中行（refField + refMap 映射），其余选中行通过 addDetailRow 直接新增明细行（每行独立映射）；表头参照保持 join 多值
  - NewVoucherDialog 明细表格补参照单元格渲染（此前仅表头支持）；PanelxForm 明细参照同样支持批量
  - 其他面板参照化（并行会话完成）：SO 表头客户→PARTNER、MO 客户编码→PARTNER、MO 材料编码→INV（过滤 属性=外购）、MO 工序编码→OP、库存单据客户→PARTNER
  - E2E 验证：新增销售订单 → 明细增行 → 点击「选择存货名称」→ 存货参照弹窗（10 条）→ 勾选 3 行 → 确定 → 明细 3 行自动生成（品牌/规格型号/单价=参考成本 10.2/8.6/18.4/含税单价 11.53/9.72/20.79 计算链全部正确）；截图 docs/ref/mes-live/43-44
  - 踩坑：el-input 渲染的单元格 textContent 为空（值在 input.value）——E2E 断言需按控件类型读值；参照字段 refField == displayField == dataName 时当前行应取单值而非 join
- [x] 细节打磨（2026-08-14）：保存后弹窗自动关闭 + 列表显示 + ElSelect 递归修复
  - 修复关键 bug：**ElSelect 无限递归渲染（Maximum recursive updates exceeded）导致列表页整表列不渲染（表头/单元格全空）**——根因：SO 查询区「客户」改为参照字段后，查询区模板直接调用 `engine.fieldOptions(qf)`（每次渲染新建选项数组）→ el-select 检测 options 引用变化 → 无限重渲染 → 渲染中断（连带 ElTabs watcher / ElTableHeader mounted 错误）
  - 修复：PanelxList 新增 `qOptions(qf)` 按列缓存选项（与 inlineOptions 同法，面板切换清缓存）；内联行参照字段双击直接弹 RefPickDialog（并行会话实现的 openInlineRef 编辑态按钮保留）
  - 验证：SO 列表 17 列正常渲染、内联空白行双击存货名称 → 存货参照弹窗 → 导入填充（CP001/铝棒 Φ80/轻铝/Φ80×3000）、新增弹窗 客户参照 + 明细存货参照 → 保存 → 弹窗自动关闭 + 「保存成功：SO-…」提示 + 列表刷新显示新单、递归 0 次
  - 协作踩坑：并行会话与本地对同一文件多次交叉修改导致 import/挂载/函数重复声明、vite transform 缓存陈旧——合并前必须读最新文件并全量 grep 重复项；E2E 选择器需避开 el-table 固定列的副本行（td 数量不同）
- 引擎支持状态：**已实现（2026-08-14）**——`engine.resolveRefOptions/fieldOptions`（下拉动态选项）、`engine.queryRefRows`（弹窗数据拉取）、`engine.refColumns/refPanelName/refLabelOf`（弹窗列/标题/显示文本）；`buildMeta` 输出 `ref`（panel/field/display/filter/map/multi/columns）；PanelxForm（表头+明细）/ PanelxList（查询区下拉 + 内联行）/ NewVoucherDialog（表头）四处已接入弹窗。新字段直接按上表配置即可，无需改组件
- 新增基础档案面板时，必须同步登记到引用登记表（下方），供其他面板引用

**引用登记表（基础档案 → 可被引用的字段）：**

| refPanel | 面板 | 常用引用字段（refField/displayField） |
|---|---|---|
| DEPT | 部门 | 部门编码/部门名称 |
| EMP | 员工 | 员工编码/员工名称（业务员/负责人/工人） |
| PARTNER | 往来单位 | 往来单位编码/往来单位名称（客户/供应商） |
| INV | 存货 | 存货编码/存货名称（产品/材料） |
| OP | 工序 | 工序编码/工序名称 |
| WC | 工作中心 | 工作中心编码/工作中心名称 |
| EQUIP | 设备 | 设备编码/设备名称 |
| TEAM | 班组 | 班组编码/班组名称 |
| WH | 仓库 | 仓库编码/仓库名称 |
| UOM | 计量单位 | 计量单位编码/计量单位名称 |
| PROJ | 项目 | 项目编码/项目名称 |
| REGION | 地区 | 地区编码/地区名称 |
| REJECT | 不合格原因 | 不合格原因编码/不合格原因 |
| ROUTE | 工艺路线 | 工艺路线编码/工艺路线名称 |
| BOM | 物料清单 | 父件编码/父件名称 |

**现有违反清单（待改造，见六-待办）：** engine.js 各面板配置中仍有静态 `options` 的字段（2026-08-14 已改造：MANU 客户编码/材料编码/工序编码、SO 客户/明细存货名称 已改参照 + 弹窗导入）：
- MANU_ORDER：生产车间（WORKSHOP_OPTIONS，暂无对应车间档案面板，保留下拉并注明）、产品编码（CP001-005）、测试程序、机构、负责人、对方仓库（WH 有档案可改）、工序名称（PROCESS_OPTIONS，工序编码参照已带出）、工作中心（WC 有档案可改）、班组（TEAM 有档案可改） 等
- SO_ORDER：结算客户（华东铝业…，客户参照已带出）、客户编码（KH001-005，客户参照已带出）、部门、业务员
- INV_CONFIGS 库存 6 单据：仓库（WH 有档案可改）、存货名称/编码（INV 有档案可改）、客户等
- BASE_CONFIGS：SYS_CODE 单据名称、SYS_PRINT 所属单据 等引用单据名的字段
- 全部改为 `dataType: '参照'` + refPanel 引用；确无对应档案的字段（如生产车间）保留下拉并在字段注释说明

### 2. 面板注册与页面概念约束

- 每张单据/档案 = 一个独立面板：`panel_config` 独立配置 + `form_data` 独立数据 + 独立路由（`/panelx/list|form/:panelCode`）+ 独立单号前缀
- 新面板必须注册进引擎注册表（`panelOf` 分发链：BASE_CONFIGS → ROUTE/BOM → INV_CONFIGS → SO/MO），禁止在组件里按 panelCode 硬编码业务分支
- 面板配置只允许在 engine.js 配置区 / init.sql 种子 / 生成器脚本中定义，页面组件（PanelxList/PanelxForm/NewVoucherDialog）保持通用

### 3. 对齐真实 T+ 约束

- 每个新模块开发前必须先实测真实 T+ 演示环境（见九-1），抓取结构存档 `docs/ref/tplus-live/`
- 面板字段/列/工具栏/状态机对齐真实面板；未实现的功能按钮保留 UI 并提示「演示环境暂未实现」，不删减
- 种子数据必须与既有面板联动（存货 CP001-005、工序 PX001-007、客户 KH001-005 等），禁止各面板各自造一套互不相关的数据

### 4. 并行开发与文件写入约束

- engine.js / PanelxList.vue 等共享文件可能被多个会话同时修改：改动前先读最新文件；写入用重试机制（文件被临时锁定时等待重试）；禁止覆盖他人未提交的修改
- 新增组件/文件先建空文件再写入（Windows 下写工具直接新建会 EISDIR）
- 改完必须 `node --check`（js）/ vite build（vue）验证语法

### 5. 验收约束

- 每个功能改动必须过 E2E（无头 Edge 9223 + localhost:5173），关键流程截图存档 `docs/ref/mes-live/`
- 前后端同步：前端 mock 配置与 `backend/src/main/resources/db/init.sql` 种子必须一致（用生成器脚本产出，避免手写漂移）


## 十二、PanelX 平台集成（2026-08-14 · 方案 B 后端代理网关）

> 承接十-1 的「B 后端代理网关」方案：平台凭据与 token 全部留在服务端，浏览器不再加载 SDK，表单引擎改走本地后端。

### 1. 平台真实 HTTP 协议（Node 假环境执行混淆 SDK + fetch 探针抓包，实测确认）

| SDK 调用 | 平台接口 | 要点 |
|---|---|---|
| `user.login` | `POST wp-core/api/user/login` | body `{userName,password}`，头 `BusDomainCode/AppCode` → `data.token`（JWT，30 天） |
| `api.getPanelConfig(code)` | `GET wp-core/api/cdp/getPanelConfig?busDomainCode=&panelCode=` | `data` 是 JSON 字符串（SDK 会 parse），含 metadata/dataSchema |
| `api.queryFormDataList(p)` | `POST wp-core/api/queryFormDataList` | body `{panelCode,pageNo,pageSize,keyword?,condition?}` → `data.{totalSize,list,privilege}`；行主键=「编号」+uuid |
| `api.getPermMatrix` | `GET wp-core/api/permMatrix?panelCode=` | `data.privilege.{actionPrivileges,fieldPrivileges,groupPrivileges}` |
| `api.getNewFormPermMatrix` | `GET wp-core/api/newFormPermMatrix?panelCode=&operationName=` | 无权限时 HTTP 400 |
| `api.getFormDescriptor` | `GET wp-core/api/formDescriptor?panelCode=&code=` | code=行「编号」（传 uuid 会 404）；`data.{data,meta}` |
| `api.callButton` | `POST wp-core/api/callButton2` | body `{panelCode,buttonName,formData,buttonParam}` |
| `api.deleteForms` | `POST wp-core/api/callButton2` | 即「删除」按钮：`buttonName:"删除"` + `buttonParam.rowCodes`（空数组被 SDK 客户端校验拒绝） |

- 公共头：`Authorization: Bearer <JWT>` + `BusDomainCode` + `AppCode`；成功封装 `{state:"200",msg,data}`，HTTP 4xx 错误体 `{errorDescription,level,errorCode,error}`
- SDK 混淆（a0_0x3bde 旋转 base64 字符串表）字符串直搜不可行 → 探针方案：Node 伪造 window/localStorage/document 后 `(0,eval)(sdk)`，拦截全局 fetch/XHR 抓包（探针脚本在临时目录 panelx-probe*.js，可复用）

### 2. 后端实现（全部新增）

- `config/PanelxProperties.java`：`mes.panelx` 配置（base-url/bus-domain-code/app-code/username/password/panel-map）
- `config/PanelxConfig.java`：`panelxRestClient`（baseUrl + 默认 BusDomainCode/AppCode 头 + 10s/30s 超时；注意 Boot 3.5 用 `ClientHttpRequestFactoryBuilder`，`ClientHttpRequestFactories` 已移除）
- `service/PanelxGateway.java`：login 换 JWT 缓存（AtomicReference）、get/post 自动带 Bearer、HTTP 401 自动重登重试一次、4xx/5xx 还原平台错误体
- `service/PanelxService.java`：7 个接口面板码映射 + getPanelConfig 的 data 字符串解析
- `service/PanelxApiException.java` + `GlobalExceptionHandler`：平台风格错误体 + HTTP 状态码透出
- `controller/PanelxController.java`：`/api/panelx/*`（getPanelConfig/getPermMatrix/getNewFormPermMatrix/getFormDescriptor/queryFormDataList/callButton/deleteForms），返回平台 envelope 原样（不包 ApiResult）
- `SecurityConfig`：`/api/panelx/**` permitAll（生产需收紧）；`application.yml`：`mes.panelx` 段 + `panel-map: MANU_ORDER→SdkTest_IML_00002`

### 3. 前端改动（极小）

- `api/index.js`：拆 `USE_PANELX_DIRECT` / `USE_PANELX_PROXY`（`VITE_PANELX_PROXY=true`），`USE_PANELX = 两者或`
- `panelx/sdk.js`：代理模式构建伪 SDK（同 user/api 子集，axios 走 `/api/panelx/*`，错误归一化为 `{response:{data:{message}}}`）；engine/adapters/PanelxList/PanelxForm 零改动

### 4. 运行方式（方案 B）

```bash
backend/build.bat && backend/run.bat          # 网关随后端启动（需能访问 demo.kwaidoo.com）
cd frontend
$env:VITE_PANELX_PROXY='true'; npm run dev    # 或写入 frontend/.env.local: VITE_PANELX_PROXY=true
```

### 5. 验证结果

- 后端 mvn clean package ✅（JDK 24 + 便携 Maven）；前端 vite build ✅
- 网关冒烟实测 ✅（MySQL 未启动也可用，Hikari 懒连接不阻塞）：`GET /api/panelx/getPanelConfig?panelCode=MANU_ORDER` → `state:200` + 测试流程面板配置（data 已解析为对象）；`POST /api/panelx/queryFormDataList` → `totalSize:18`，首行 `Do_not_change_sample_111`（与 SDK 直连探针一致）
- 待办：无头 Edge 走 vite 代理全链路 E2E + 截图存档 docs/ref/mes-live/；生产化前收紧 `/api/panelx/**` 鉴权


## 十三、前端分层重构（2026-08-14 · 通用引擎层/业务层分离）

> 目标：把「写一次处处用」的引擎代码与 MES 业务代码分开，为抽包复用打地基。详见 docs/架构分层.md。

### 1. 新结构

```
src/core/      通用引擎层（跨项目复用）：env.js 模式开关 / request.js / sdk.js / panel-engine.js 内核 / views 引擎视图
src/business/  业务层：engine.js（面板配置+注册表+mock+平台码映射）/ api.js / menus.js
src/layout|views|stores|router  壳层与业务页面（暂留原位，phase 2 再拆）
```

- vite.config.js 新增别名 `@core` → src/core；core 内禁止 import 业务文件（规则见 core/README.md）
- engine.js 内核函数（unwrap/errMsg/mapType/parseJson/adaptPanelConfig/adaptMeta/adaptFormData/panelxButtonGroups/platformCall）抽入 core/panel-engine.js，engine.js 头部改为 import + re-export，其余业务配置原样保留
- 验证：vite build ✅（chunk 体积与重构前一致：engine 231.09 kB / PanelxList 28.63 kB / PanelxForm 21.34 kB）

### 2. 事故记录（批量脚本污染 + 修复，教训必读）

- 过程：用 PowerShell 批量替换 import 路径时，单元素数组被 PowerShell 展开为扁平字符串，导致 `$pair[0]`/`$pair[1]` 取到的是字符串首两个字符，把 12 个文件里所有字母 f 替换成了 r（from→rrom、if→ir、v-for→v-ror、typeof→typeor、'\ufeff'→'\urerr'…）
- 修复：确定性单词级映射表（90+ 词条：ror/ir/rer/runction/rorm/rield/rilter…）+ 大小写变体（Rer/Ref、Der/Def、LertNav/LeftNav、Inro/Info、typeor/typeof、indexOr/indexOf、rly/fly、rit/fit、#rrr/#fff、rpd 为原名保留）+ 逐轮 vite build 纠错，最终 build 通过且产物体积与污染前一致
- 教训：① 批处理文件替换禁止用「数组 of 数组」构造（用 here-string `旧|新` 行格式 + 索引步进）；② 任何批量写入前先 git/复制备份（本项目无 git，差点无法恢复——强烈建议 `git init`）；③ 大批量修改后必须全量构建 + 产物对比验证

### 3. 待办（phase 2/3，见 docs/架构分层.md 第五节）

- core/ 抽独立包（npm workspace / light-mes-core）；后端引擎抽 mes-engine-starter
- 拆耦合：core/views 对 business/engine 的依赖改为注入；sdk.js 的 PANELX_CONFIG 默认值改为业务覆盖
- 本项目建议尽快 `git init` + 首次提交（避免再次裸奔）


## 十四、T+ 抓取管线工具化 + Vision Toolkit 落地（2026-08-15）

### 1. DSH Vision Toolkit 插件安装与调通

- `dsh plugin --profile web add -w @anionex/dsh-vision-toolkit`（v0.1.7，10 个视觉工具 + vision-tools 技能 + Web 设置页）
- 排障三连：① pip 拉包失败 → 写 %APPDATA%\pip\pip.ini 阿里云镜像；② **Store 版 Python 的 venv 启动器在插件隔离环境（HOME/LOCALAPPDATA 重定向）下无法创建进程（ensurepip 101）** → 装 python.org 官方版 3.13.14（%LOCALAPPDATA%\Programs\Python313），插件设置 runtime.python 指定之；③ 免费 glm-4v-flash 限 max_tokens≤1024，ground/detect 工具报 1210 错 → 换百炼 qwen-vl-max（grounding 恢复）
- Node 升级 v24.19.0（用户级 + 机器 PATH 置顶，explorer 缓存需重启）

### 2. 真实 T+ 登录 + DOM 级复刻管线（tools/tplus-grab）

- 纯 CDP 无依赖（Node ≥21 WebSocket）：grab.cjs（Edge 复用/启动 + expBtn→selectRoles#mes 演示登录 + 单据页抓 col[t]/查询字段/页签/工具栏/计算样式 + 截图 + 生成骨架 HTML）、gen.cjs（dom.json → HTML）
- 用法见 tools/tplus-grab/README.md；会话 cookie 存 profile 免重复登录
- 实测：生产加工单 MP05 骨架复刻像素差异 4.1%（纯视觉法 7.4%）；真实设计值：白底/12px/工具栏黑字14px（新增非蓝色）/页签蓝 #3788FF 加粗/标题红 #FF0033/表头 #E0E0E0/21 可见列真实宽度/查询字段 机构·合同号·锭号·批号·生产车间·重量·预开工日·预完工日…
- 演示账号每次分配不同（本次 tplusdemo12855，账套 [012007]）

### 3. 待办

- 复刻差异继续压（工具栏间距/下拉箭头图标等像素细节）；新模块开发前跑管线拿真实结构
- Vision 配置：百炼 key 在 DSH 凭据库 VISION_API_KEY；免费 glm-4v-flash 可作日常低成本档
