# 轻MES 开发规范与需求总览

| 属性 | 内容 |
|---|---|
| 文档类型 | 开发规范 |
| 适用场景 | 通用架构与 SDK |
| 维护状态 | 生效 |
| 最后整理 | 2026-08-24 |
| 文档导航 | [文档中心](README.md) |

> 本文档是开发约束与需求速查；完整场景分类和文档导航见 [文档中心](README.md)。
> 内容更新：2026-08-24

---

## 一、项目概览

- **定位**：参照畅捷通 T+ 门户形态的网页端轻量 MES（生产制造执行系统）。
- **技术栈**：Vue3 + Element Plus + Pinia + Vue Router（前端）｜Spring Boot 3.5 + MyBatis-Plus + MySQL + JWT（后端）。
- **数据源模式**（前端 `core/env.js` 环境变量切换；2026-08-20 起仅两模式，Mock / PanelX 直连已移除）：
  | 模式 | 环境变量 | 说明 |
  |---|---|---|
  | 本地后端（默认） | `VITE_PANELX_PROXY` 空 | `/api` 代理 → Spring Boot（8080）+ MySQL（3308） |
  | PanelX 后端代理 | `VITE_PANELX_PROXY=true` | 后端网关直连平台（token 不落浏览器）；门户壳仍走演示数据 |
- **开发环境**：Node v24 / JDK（build.bat 自动探测）/ 便携 Maven 3.9.9（`tools/`，阿里云镜像 `tools/settings.xml`）。
- **运行**：前端 `cd frontend && npm run dev`（5173）；后端 `backend/build.bat && backend/run.bat`（8080）；数据库连接以 `application.yml` 为准（当前默认 3306）。`start-project.bat` 仍按本地 MySQL 3308 检测，使用前必须与后端配置对齐。
- **目录结构**（`frontend/src/`）：
  ```
  core/      通用引擎层（跨项目复用，禁止 import 业务）：env/request/sdk/panel-engine + views 引擎视图
  business/  业务层：engine.js（面板接口 + 平台映射）/ api.js / menus.js
  layout/    门户壳：TopBar / LeftNav / TabsBar / NoticeCenter / HelpPanel / InitWizard
  views/     业务页面：login / dashboard / modules（看板/返修台/方案中心）/ sys（组织架构）
  stores/    user（业务）/ tabs、app（通用）
  router/    路由 + 守卫（isLogin / requireAdmin）
  ```

---

## 二、开发规范（速查汇总）

### 2.1 架构分层规范（详见 docs/架构分层.md）

- 依赖方向单向：`business → core`、壳层 → business/core，**core 禁止 import 业务**。
- 新面板 = 配置（business/engine.js 或后端 panel_config 表），页面组件零改动；面板注册表 `panelOf` 分发链：BASE_CONFIGS → ROUTE/BOM → INV_CONFIGS → SO/MO。
- 维护规则：业务代码禁止写进 core；任何改动验证 = 前端 `vite build` + 后端 `mvn package`。
- 抽包路线：Phase 1 已分层（别名 `@core`）；Phase 2 core 抽独立包；Phase 3 配置外部化。

### 2.2 页面结构规范（详见 docs/页面开发规范.md）

面板列表页固定 4 层（仅单据类有第 ④ 层）：

```
① 顶部工具栏（灰条 #f5f7fa）：按钮分组（name+actions，点组名=执行首 action，>1 显示 ▼ 下拉）
   右：单据翻页 ◁ ◀ ▶ ▷ + 第 x/y 张 + 单据编号 + 状态徽章
② 表头字段区（label 上、输入框下；2 行；宽 160px；回车即查询）← tablePages[0].queryFields
③ 明细区块（gridTabs/detail.tabs）：页签 + 表格固定 5 行 + 合计行 + 汇总页签
④ 表尾（仅单据类，sticky 底部）：备注（可编辑）+ 审核行（制单人/审核人/审核日期/审核时间/打印次数/创建时间）
```

- **数据契约**：单据类列表查询返回单据级行 `{编号, 单据状态, ...表头, detail:{<tabKey>:[明细行]}}`；档案/报表类平铺行。
- **配置契约**：`buttonGroups[{name, actions}]`（禁止 items/caret 别名）、`gridTabs[{label,rowSource,columns,summary}]`、`detail.tabs[{key,label,fields,summaryItems,calc}]`（key 与 detail 键一致）、`queryFields`。
- **交互约定**：表头查询回车/change/清空即搜；右键菜单（定位/复制/另存EXCEL/批量修改）；双击明细行开表单；固定 5 行 + 占位行；表尾 sticky；列宽按 dataType 推算（是否 74/图片 56/数值 104/日期 136/文本 96-220）。
- **单单据面板**（13 个：EMP/DEPT/INV_PRICE/EQUIP/WC/OP/WH/REGION/PROJ/UOM/REJECT/ROUTE/BOM，详见 页面开发规范 §八）：`metadata.singleDoc=true`、dataSchema.fields 只留单据头、明细全放 detail.tabs、gridTabs 列=明细行字段、只允许 1 张 form_data（form_no=面板名）、保存=整张明细覆盖写回、编号=最大序号+1 查重。

### 2.3 面板交互标准（详见 docs/design/面板交互设计规范.md，19 节）

| 节 | 主题 | 要点 |
|---|---|---|
| §1 | 面板形态 | 列表（PanelxList）/表单（PanelxForm）/报表只读 |
| §2 | 点击填写 | **新增=列表页直接创建最新草稿单内联填写（2026-08-24 定稿，不再弹新增弹窗）**：autoCode 编号+单据日期=当天自动填表头；表头 2 列网格、表单页 3 列网格；dataType→控件映射；必填红星；autoCode 只读；只读规则=编辑态非草稿禁用；udl-fields 下划线风格 |
| §3 | 弹窗标准 | 消息反馈四色（成功/警告/info 未实现/错误）；确认弹窗；拉式选单弹窗（selectConfig） |
| §4 | 工具栏 | 分组渲染、快捷键（Alt+S 保存等）、按钮按状态可用性、未实现项 info 提示。**灰按钮点击拦截（2026-08-25 强制）**：isDisabled=true 的按钮必须在前端入口拦截——PanelxList/PanelxForm 的 onButton/onGroupAction 开头 `if (isDisabled(action)) return`，点击不得触发任何操作/提示词/后端调用；disabled 仅作视觉（.tb-main.disabled / .ctx-item.disabled / el-dropdown-item :disabled），禁止只靠视觉置灰不拦事件 |
| §5 | 列表页 | 查询区、明细/汇总双子页签、行内新增、分页 |
| §7 | T+ 对齐 | 选单前提=已审核未中止、自动编号、未实现按钮保留 |
| §8 | 审批流 | 状态机：草稿→提交审批→审批中→通过(已审核)/驳回(草稿)；form_approval 留痕；审核人=当前登录人；按钮可用性矩阵 |
| §9 | 单据编号 | 前缀-yyyy-MM-dd+2位当日序号；精确查重循环+唯一索引 uk_panel_formno；前端不生成 |
| §10 | 生单/选单 | 拉式选单（headerMap/detailMap）与推式生单（createXxxFromYyy + gotoPanel） |
| §11 | BOM 联动 | **BOM 关系唯一数据源=物料清单面板 children（2026-08-25 起，原存货 INV _bom 已废弃迁移）**；产品编码变更带出材料（子件BOM=产品编码）；推式生单带材料；子件编码/父件编码参照 INV |
| §12 | 删除/存货类别 | 删除组双动作（勾选删明细行/删整单）；INV 固定 5 张类别单据（CP/YL/FZ/BZ/BC） |
| §13 | 产成品→材料过滤 | 子件BOM 标记精确过滤优先、材料编码兜底；默认选中首行；两入口双路径点击 |
| §14 | 常见坑 | 业务主入口=列表页流览；数据契约四处带子件BOM；@click.capture 双路径；界面严禁卡通/emoji；JWT 随机密钥+403；401/403 前端登出 |
| §15 | 材料下级 BOM | 有下级材料编码右上角红 *，点击弹 SubBomDialog（只读，不递归） |
| §16 | 角色权限 | sys_role + sys_role_panel（can_approve）+ sys_user.role_id；admin 超级权限；菜单过滤 flatMenus；审批按钮按 approvePanels 过滤；性能缓存 TTL 60s/30s |
| §17 | 产成品入库单 | 对齐真实 T+ ST1002 的表头 7 字段/明细 13 列/流转 |
| §18 | Excel 导入 | ImportDialog + SheetJS；字段名精确匹配；档案面板=每行新建档案（archNo）；零列禁止导入 |
| §19 | 我的桌面 | DashboardController /api/dashboard/stats 聚合接口驱动，KPI/进度/待办/单据统计/档案概览/最新单据 |
| §20 | 工具栏按钮组统一设计 | **底层标准（2026-08-20）**：审核组固定六动作一组（审核/弃审/审批情况/提交审批/审批通过/审批驳回，对齐 SO_ORDER），禁独立审批组/审批情况组；导入/打印/更多/查找通用能力标准；23 个单据面板已统一 |
| §21 | 生单/选单联动（2026-08-24，调拨单 2026-08-25，进货单 2026-08-25） | 业务流转：请购单→采购订单→采购入库单/进货单→采购发票；销售订单→销货单/销售出库单/配货单/生产加工单；加工单→工序汇报单/领料申请→材料出库单/产成品入库单/调拨单；领料申请单→调拨单/材料出库单；调拨单→材料出库单（调拨出库）；采购需求分析（PU_REQ_ANALYSIS）聚合加工单材料需求-现存量-已请购-已出库=建议请购数量；选单 selectConfig 15 面板已覆盖；推式生单 9 条链路（insertGenerated 统一落库 + fillNewDefaults 必填兜底：锭号/单据编号/单据日期）；生单展示=直接跳目标面板列表页置顶新单（不新开标签页/弹窗）。**选单/生单前提=源单据已审核（2026-08-25 强制四层保障）**：① 选单弹窗 SelectVoucherDialog 查询恒带 condition「单据状态=已审核」；② 字段参照 engine.queryRefRows 对配置含「审核」动作的面板（单据类参照）自动附加已审核过滤（档案面板无审核流程不受影响）；③ PanelxList/PanelxForm 所有「生成XX」按钮 isDisabled 统一按源单状态置灰（仅已审核/生产中可用，生产中=已审核后续状态，允许生成工序汇报/产成品入库）；④ 后端推式生单接口（createXxxFromYyy）一律校验源单状态，未审核抛「仅已审核XX可生成YY」 |
| §22 | 业务总览（2026-08-24，2026-08-25 补调拨单/采购需求分析/委外管理） | BusinessOverview.vue：智能供应链→业务总览；7 模块（生产/委外/采购/销售/配货/库存/质量）SVG 流程图+相关单据/档案/报表；节点点击直达面板；新增面板同步维护 modules 数组；侧栏叶子模块直接跳转；生产管理流程图 9 节点含调拨单（加工单/领料申请→调拨→材料出库）与采购需求分析（加工单→需求分析→请购单）；委外管理模块：委外加工单(products+materials 双明细)→委外发料单/委外入库单/委外加工费用单 + 8 张委外报表 |

### 2.4 开发约束（强制，详见 工作记录.md §十一）

1. **字段引用基础数据**：语义上对应基础档案的字段必须用「参照字段」（`dataType:'参照'` + refPanel/refField/displayField/filter/refMap/refColumns/refMulti）；仅基础档案无对应语义时才允许手写 options（需注释说明）。引用登记表见工作记录 §十一-1（DEPT/EMP/PARTNER/INV/OP/WC/EQUIP/TEAM/WH/UOM/PROJ/REGION/REJECT/ROUTE/BOM）。
2. **面板注册**：每单据=独立面板（panel_config + form_data + 独立路由 + 单号前缀）；新面板注册进 `panelOf`；禁止组件内按 panelCode 硬编码业务分支。
3. **对齐真实 T+**：新模块开发前先实测真实 T+ 抓结构存档 `docs/ref/tplus-live/`；未实现按钮保留 UI 提示「演示环境暂未实现」；种子数据与既有面板联动（CP001-005/PX001-007/KH001-005 等）。
4. **并行开发**：engine.js/PanelxList.vue 等共享文件可能多会话并发修改，改前读最新、写入校验唯一性、禁止覆盖他人未提交修改。
5. **界面风格**：严禁卡通/emoji/动画装饰；T+ 专业商务风；文案中性。

### 2.5 验收与测试规范

- 每个功能改动必须过 **E2E**（无头 Edge 9223 + localhost:5173，CDP 脚本），关键流程截图存档 `docs/ref/mes-live/`。
- 前端改动 `node --check`（js）+ `vite build`（vue）；后端 `mvn package`；不通过不提交。
- 面板配置三端一致：`panel_config` 表（DB 为最终来源）↔ init.sql ↔ 引擎接口（2026-08-20 前端 mock 配置已移除，不再存在前端种子）。
- E2E 登录注入要点：CDP 注入 localStorage 后必须导航「带 query 的 URL」强制整页加载；真实模式需注入真实登录 token（假 token 会 401 自动登出）。
- 涉及中文数据的自动化写操作与诊断一律用 Node 脚本（PowerShell 中文经管道/HTTP 会编码污染）。

### 2.6 工具与流程

- **T+ 真实面板抓取**：`node tools/tplus-grab/grab.cjs --login --url <Voucher.aspx URL> --name xx --out docs/ref/tplus-live/xx`（产物 dom.json/png/html；会话存 profile/）。
- **本地验证**：`tools/tplus-grab/verify.cjs --url http://localhost:5173/#/panelx/list/<CODE>` 逐页签核对；`shot.cjs` 截图。
- **无头浏览器**：9222（T+ 抓取）/ 9223（本地 E2E），puppeteer-core 在 %TEMP%\pptr。
- **种子生成**：dsh-edit/gen-sql.cjs（vm 提取 engine.js 配置 → init.sql）；apply-edit.cjs（edit 工具 EIO 回退）；patch-*.cjs（批量注入补丁）。
- **git**：`tools/git-auto-push.cmd` 自动 commit+push（GitHub origin，master）。

---

## 三、需求清单

### 3.1 已完成需求（里程碑）

**门户壳（T+ 保真）**
- 顶栏三段式（Logo/工厂切换/账号认证/登录日期/服务到期/搜索/公告/角标/帮助/用户下拉）；三级悬浮导航；多页签（path+query 恢复、keep-alive）；帮助面板；初始化向导；登录页+路由守卫+JWT；我的桌面。

**面板体系（45 菜单全开发完成）**
- 自研元数据表单引擎（Panelx 体系，配置驱动）：生产加工单 MANU_ORDER（三明细 21/24/44 列 + 工序材料子表 + 工具栏 12 组，对齐 T+ MP05）、销售订单 SO_ORDER、库存 6 单据（采购/产成品/其他入库、销售/材料/其他出库）、工序汇报单 PROCESS_REPORT（61 列明细）、返修工序汇报单。
- 基础档案 13 + 关联设置 3 + 价格本 + 双列表 2（ROUTE/BOM）+ 查询 2（BOM_FWD/BOM_REV）+ 系统设置 12 + 初始化 3。
- 报表 31（16 库存报表 + 12 生产/销售报表 + 3 专属视图：生产看板/返修工作台/方案中心）。

**业务能力**
- 参照字段弹窗（RefPickDialog，多选批量导入）、选单/生单（SO→MO→PR/FI 流转）、审批流（提交/通过/驳回/历史，15 面板含 PU_REQ）、BOM 联动（产成品→材料 + 材料下级 BOM 红 *）、Excel 导入（SheetJS）、单单据档案（13 个：员工/部门/存货价格本/设备/工作中心/工序/仓库/地区/项目/计量单位/不合格原因/工艺路线/物料清单）、角色权限（组织架构三栏）、Dashboard 真实看板、存货单单据模型（2026-08-24 改）、编号安全生成（最大序号+1 查重）。
- **请购单 PU_REQ（2026-08-24）**：T+ 机械行业实测开发，表头 20 字段/明细 21 列/查询 10 字段/选销售订单/生采购订单。
- **业务流转全链路（2026-08-24，调拨单 2026-08-25）**：推式生单 8 条（请购→采购订单→采购入库、销售订单→销售出库/生产加工单→工序汇报/产成品入库/领料申请→材料出库、领料申请→调拨单、调拨单→材料出库（调拨出库））+ 选单 selectConfig 15 面板覆盖；生单直接跳目标面板列表页置顶新单（不新开标签页）。
- **业务总览（2026-08-24，2026-08-25 更新）**：智能供应链→业务总览，7 模块 SVG 流程图 + 相关单据/档案/报表直达；生产管理流程图补调拨单（TRANSFER，DB- 前缀）与采购需求分析（PU_REQ_ANALYSIS：加工单材料需求-现存量-已请购-已出库=建议请购数量）。
- **委外管理模块（2026-08-25）**：对齐真实 T+ 委外管理 OM 模块——委外加工单 OUTSOURCE_ORDER（WW- 前缀，products 产成品 + materials 委外材料双明细，选销售订单）→ 生成委外发料单 OUTSOURCE_ISSUE（WF-）/委外入库单 OUTSOURCE_IN（WR-）/委外加工费用单 OUTSOURCE_FEE（WY-，按委外单价×数量汇总费用）；8 张委外报表（产成品/材料/费用明细表、执行表、发料耗用结存表、产成品/材料/费用统计表）。
- **销售管理单据链（2026-08-25）**：对齐真实 T+ 销售管理 SA 模块单据链——报价单 QUOTE_ORDER（BJ- 前缀）→ 生成销售订单（SO_ORDER 运行时注入 selectConfig「选报价单」）；销货单 SALE_INV → 生成销售发票 SALE_INVOICE（FP- 前缀，按含税单价拆分无税单价/税额/价税合计，选销货单）；费用单 EXPENSE（FY- 前缀）→ 生成销售费用分摊单 SALE_COST_ALLOC（FT- 前缀，选费用单）。
- **采购管理单据链（2026-08-25）**：对齐真实 T+ 采购管理 PU 模块单据链——进货单 PU_IN/采购订单 PU_ORDER → 生成采购发票 PU_INVOICE（PI- 前缀，按含税单价拆分税额/价税合计，选进货单）；费用单（费用类型=采购费用）→ 生成采购费用分摊单 PU_COST_ALLOC（PC- 前缀，费用类型校验拒绝非采购费用，选费用单）。
- **移动仓管与序列号管理（2026-08-25）**：对齐真实 T+ PDA/SN 模块——移动仓管介绍页（入库→上下架→出库→货位调整流程 + 单据/报表快捷入口）；库存盘点单 STOCK_CHECK（PD- 前缀，账面/实盘/盈亏）+ 货位调整单 LOCATION_ADJUST（HW- 前缀，原货位/新货位）；序列号管理介绍页（启用→入库登记→出库扫描→追溯）；序列号登记单 SERIAL_NO（XL- 前缀，每行一个序列号）+ 序列号状况表 SERIAL_STATUS（仅在库）+ 序列号跟踪表 SERIAL_TRACE（全量带出入库追溯，后端展平 SERIAL_NO 明细）。
- **生产/配货报表补齐（2026-08-25）**：对齐真实 T+ 生产管理 MP 与配货管理 DIM 模块报表——生产加工单执行表 MANU_ORDER_EXEC（计划/汇报/完工/入库/进度%）、跟踪工具 MANU_ORDER_TRACKER（工序级报工/合格/进度%）、产成品明细/材料明细表 MANU_ORDER_PRODUCT_DETAIL / MANU_ORDER_MATERIAL_DETAIL、产成品统计/材料统计表 MANU_ORDER_PRODUCT_STATS / MANU_ORDER_MATERIAL_STATS；配货单明细表 PICK_ORDER_DETAIL、配货单统计表 PICK_ORDER_STATS、配货综合统计表 PICK_ORDER_SUMMARY（配货-销售出库-未出库）。

**平台集成**
- PanelX 后端代理：浏览器不加载平台 SDK，统一通过 `PanelxGateway` 自动登录、缓存 JWT，并在 HTTP 401 时自动重试一次。

### 3.2 待办需求（以 工作记录.md §六 为准，2026-08-19 复核）

- [ ] 单据设计器页面：可视化编辑 panel_config（字段/列/按钮/明细），对接 savePanelConfig。
- [ ] 待办/消息/预警角标接真实数据（当前门户代理模式使用演示数据）。
- [ ] 剩余面板字段 options 改造为参照（违反清单见 工作记录 §十一-1：MANU 产品编码/对方仓库/工作中心/班组、SO 部门/业务员、库存 6 单仓库/存货、BASE_CONFIGS 引用单据名等）。
- [ ] 权限矩阵进一步细化（角色 CRUD 已完成；面板级字段权限 fieldPrivileges 尚未生效）。
- [ ] 生产加工单「审核后联动 MANU_ORDER 工序完工状态」待真实接口验证。
- [ ] `VITE_PANELX_PROXY=true` 模式下门户壳仍使用演示数据，待接入真实菜单/角标。

### 3.3 已知事项与技术债（2026-08-19 复核）

- ~~真实模式参照字段渲染为下拉~~（**2026-08-20 已修复**：`PxService.buildMeta` 透传参照信息 ref{panel,field,display,filter,map,multi,columns}，表头参照弹窗恢复，实测 SALE_OUT 经手人 → 员工 8 行）。
- **PROCESS_REPORT 单据日期 defaultValue 陈旧**：DB panel_config 为 '2026-08-14'（engine.js 是 today），新建单默认日期偏旧，待种子同步。
- **员工档案明细行编号缺失**：员工明细行无统一编号生成（EMP-001 为种子，导入/新增行无编号规则），待定。
- init.sql 重放不清理存量 form_data（残留行由 ON DUPLICATE 处理，全量重建需 drop 库）。
- 后端 JWT 密钥已在 application.yml（生产需更换）；`/api/panelx/**` 生产需收紧鉴权。
- 演示账号密码 BCrypt 种子、Lombok 已移除（手写 getter/setter）、MyBatis-Plus 分页需 jsqlparser 依赖。
- **三套存货编码体系并存（2026-08-25 BOM 迁移时实测确认）**：① 本地运行库（phpstudy 3308）= YL/FZ/BZ/BC 体系 17 物品（5 类别单据 CP-001/YL-001/FZ-001/BZ-001/BC-001）；② 服务器运行库（8.134.255.221）= CL 体系 10 物品（产成品 CP001-005 + CL001-005，4 类别单据 CP-001/CL-001/FZ-001/BZ-001）；③ init.sql 种子 = CL 体系 10 物品（旧单单据 INV-001~010 行）。**BOM children 子件编码必须引用各自运行库存在的存货编码**——部署 SQL 前先核对目标库存货体系（服务器迁移时曾因用本地 YL 编码覆盖服务器 BOM 导致悬空，已按 CL 体系重建 12 行）。

---

## 四、历史事故红线（BUG 速查，详见 docs/BUG总结.md）

1. **批量替换事故（f→r）**：PowerShell 数组嵌套展开导致 12 文件 800+ 处 f→r；修复=单词级映射+逐轮构建+产物对比。教训：批量替换禁止「数组套数组」；改前 git 提交。
2. **编号 COUNT 法撞号**：EMP 导入 Duplicate entry（COUNT+1 与断号冲突）；修复=最大序号+1 查重循环。**编号生成一律禁止 COUNT 行数法**。
3. **单单据 gridTabs 列配错**：列配成单据头字段→明细预览全空→保存整张覆盖丢数据。**渲染异常时禁止保存**。
4. **汇总分组行数值翻倍**：先复制首行再累加同字段。聚合前先清空待聚合字段。
5. **保存清空明细**：列表页保存仅传 {编号} 时 detail 被 toJson(null) 覆盖。明细兜底=未传 detail 保留原数据。
6. **ElSelect 无限递归**：查询区参照字段每次渲染新建选项数组。选项按列缓存。
7. **ESM re-export 悬空**：`export ... from` 不创建本地绑定。文件内部要用需显式 import。
8. **页签丢 query**：生单后切页签数据丢失。页签=path+query，切换禁止只 push path。
9. **选单只能选第三张单/表单页选单不可用**：接口须返回 selectConfig；?select=1 需 watch payloadCache。
10. **PowerShell 中文/特殊字符污染**：写操作与诊断一律 Node 脚本；mysql 加 --default-character-set=utf8mb4。
11. **MySQL JSON path 不支持中文键**：JSON_SEARCH 需全文档搜索（如 EMP 脏行清理）。
12. **CDP 会话坑**：evaluate 超时竞速 + 点击 fire-and-forget + 轮询重试；登录注入后必须整页加载（带 query 导航）；真实模式假 token 401 自动登出。
13. **灰按钮点击穿透（2026-08-25）**：`disabled` 只是视觉 class，点击事件仍触发 onButton→调后端→弹「仅已审核…」提示（用户视为 bug）。修复=onButton/onGroupAction 入口统一拦截 isDisabled（不执行/不提示/零后端请求）。**新面板按钮一律走 isDisabled 状态矩阵 + 入口拦截，禁止裸视觉置灰**。

---

## 五、文档导航

- 按开发场景分类的完整索引见 [文档中心](README.md)。
- 文档编码、字体、标题、排版和日志边界见 [文档编写规范](文档编写规范.md)。
