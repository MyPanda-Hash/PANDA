# T+ 轻MES 生产加工单 · 真实面板设计（实测提取）

| 属性 | 内容 |
|---|---|
| 文档类型 | 场景设计 |
| 适用场景 | 生产制造 |
| 维护状态 | 调研基线 |
| 最后整理 | 2026-08-24 |
| 文档导航 | [文档中心](../README.md) |

> 来源：畅捷通 T+ 体验站实测（2026-08-14 抓取）
> 入口：https://t.chanjet.com/tplus/view/login.html → 立即体验 → 选领域「新制造」→ 选行业「轻MES」
> 演示账号：tplusdemo12852（系统自动分配），账套域 h2t.chanjet.com
> 单据页 URL 模式：`/tplus/BAPView/Voucher.aspx?sysId=mp&mId=mp05&pId=voucherView|...`
> 截图见同目录 `tplus-live/`，原始 DOM/文本见 `tplus-live/*.txt`

## 1. 菜单（真实 T+ 轻MES 账套）

- 新生产 → 生产管理 → 单据：**生产加工单**、生产加工单工序统计表
- 新生产 → 生产管理 → 报表：生产加工单产成品明细表、材料明细表、工序明细表、产成品统计表、材料统计表、工序统计表、执行表、生产在制看板、生产库存看板、生产运营看板
- 新生产 → 智慧车间 → 单据：工序汇报单、返修工序汇报单、返修工作台；报表：工序汇报单明细表/统计表、工序进度表、工序状况表、工资核算单明细表/统计表

## 2. 列表页（voucherView）

### 查询区字段（自上而下）
合同号*（日期型）、锭号*（=单号，自动 MO-yyyy-MM-####）、批号*、生产车间*、预开工日、预完工日、销售订单号、客户编码、客户、测试程序*、生产订单客户

### 表格视图
- 网格页签：**产成品明细 / 产成品明细汇总**（列表即按产成品明细行展示）
- 网格右键：定位 / 复制到剪贴板 / 从剪贴板粘贴 / 另存为EXCEL模板 / 批量修改 / 销售订单查询 / 存货中心 / 更多 / Ctrl+V列粘贴

## 3. 表单页（新增态，voucherStateControl=New）

### 工具栏（真实按钮组，完整提取）
- **新增**：新增 / 引入常用单据 / 设置默认功能
- **选单**：选销售订单
- **保存**：保存(Alt+S) / 保存新增(Alt+\) / 保存为草稿 / 保存为常用单据 / 保存打印(Alt+G)
- **删除**、**审核**
- **生单**（11 项）：生成材料出库单、生成材料出库单(分单)、生成材料出库单(退料)、生成工序汇报单（自制汇报）、生成工序汇报单（委外汇报）、生成工序汇报单（自制汇报退回）、生成工序汇报单（委外汇报退回）、生成产成品入库单、生成产成品入库单(退库)、生成补投生产加工单（按累计报废入库数量）、生成返工生产加工单（按累计报废入库数量）
- **变更**
- **工具**：现存量查询 / 变更历史 / **联查**（执行情况、销售订单情况、材料出库情况、工序汇报情况、产成品入库情况、加工单执行图、返工/补投加工情况、生单流程联查）
- **设置**：单据设置 / 移动控件位置 / 调整控件宽度 / 工具栏设置
- **打印**：直接打印、打印、预览、打印模板设置、导出、明细标签打印、工序流转卡打印、打印情况
- **更多**：复制 / 放弃(Alt+Z) / 草稿 / **中止执行** / **重取BOM材料（仅追加）** / 附件 / 刷新 / 消息

### 表头（3 列网格，10 行，含绝对定位坐标实测）
| 行 | 左 | 中 | 右 |
|---|---|---|---|
| 1 | *合同号（日期型，默认今天） | *锭号（=单号 MO-2026-08-0009） | *批号（下拉参照） |
| 2 | *生产车间 | 预开工日 | 预完工日 |
| 3 | 销售订单号 | 客户编码 | 客户 |
| 4 | *测试程序（行业自定义项） | 测试程序 | 生产订单客户 |
| 5 | *机构（MarketingOrgan，必填） | 重量 | 开工日期 |
| 6 | 完工日期 | 启用派工（勾选） | 自动转移（勾选） |
| 7 | 产品自动添加到材料（勾选） | 是否手工修改单据编码（勾选） | — |
| 8 | 销售订单ID | 是否变更操作 | 外部单据号 |
| 9 | 是否只读 | 负责人 | 启用领料申请（勾选） |
| 10 | 对方仓库 | … | … |

表尾（底栏）：制单人 / 审核人 / 审核日期 / 审核时间 / 打印次数 / 创建时间 / 修改人 / 变更人 / 变更日期 / 审核机器人

### 表体：三明细分录 + 每明细「明细/汇总」双子页签
配置元数据：`multidetailsudi="137188产成品明细 137191材料明细 137390工序明细"`

#### ① 产成品明细（21 列）
生产类型 / *产品编码 / 存货图片 / *产品名称 / 规格型号 / 型号 / 适用BOM / BOM展开方式 / *生产单位 / *数量 / 齐套数量(主) / 累计汇报套数(工序单位) / 可用量 / 可用量说明 / 现存量 / 现存量说明 / 产品字符公用自定义项1 / 图号 / 单重 / 总重 / 需求令号

#### ② 材料明细（24 列）
*材料编码 / *材料名称 / 存货图片 / 规格型号 / 子件BOM / 预出仓库 / **材料倒冲方式**（下拉） / **领料工序** / **允许循环** / **行中止** / *计量单位 / 定额生产数量 / 定额需用数量 / 定额需用数量2 / 需用数量 / 损耗数量（损耗率%为隐藏列）/ *计划数量 / 累计领用数量 / 可用量 / 可用量说明 / 现存量 / 现存量说明 / 单重 / 总重

#### ③ 工序明细（43 列，含返修/计件工资/委外）
工序行码 / 工艺类型 / 工艺序号 / *加工顺序 / 加工类型 / 工序编码 / 工序名称 / 工序备注 / 生产车间 / 工作中心 / 设备 / 班组 / 工人 / 委外供应商 / 委外单价 / 税率% / 委外金额 / 按辅单位计价 / 计价辅单位 / 换算率(辅单位) / 计价辅数量 / **工序完工状态** / **手工完工** / 行中止 / 工价（辅单位）/ **工废工价** / **工废工价（辅单位）** / **料废工价** / **料废工价（辅单位）** / 工序单位 / 计划数量 / **工资类型** / **工价** / **金额** / **关键工序** / **单位标准工时** / **待返修数量-本序发现** / **待返修数量-他序发现** / 工序字符专用自定义项1 / 计划时间 / 完成时间 / 单重 / 总重 / 需求令号
（工序网格尾部还带本工序材料子表：材料编码/材料名称/规格型号/计量单位/需用数量/损耗数量）

## 4. 与轻MES 当前 MANU_ORDER 的差异与改造建议

| 维度 | 真实 T+ 轻MES | 轻MES 现状 |
|---|---|---|
| 锭号 | = 单据编号（MO-2026-08-0009 自动） | 手工录入的锭编号 |
| 合同号 | 日期型字段 | 文本 |
| 表头 | 28+ 字段（机构*/重量/开工/完工/启用派工/自动转移/产品自动添加到材料/外部单据号/启用领料申请/对方仓库…） | 17 字段 |
| 产成品明细 | 21 列（生产类型/型号/适用BOM/BOM展开方式/生产单位/齐套数量/累计汇报套数/可用量/现存量/图号/单重/总重/需求令号） | 12 列 |
| 材料明细 | 24 列（子件BOM/材料倒冲方式/允许循环/行中止/定额需用数量2） | 15 列 |
| 工序明细 | 43 列（工作中心/设备/委外价税/工序完工状态/手工完工/行中止/工废·料废工价/工资类型/关键工序/标准工时/待返修数量/计划·完成时间 + 工序材料子表） | 14 列 |
| 子页签 | 每明细有「明细/汇总」双视图 | 汇总为独立页签 |
| 工具栏 | 选单/保存新增/保存为草稿/保存打印/生单11项/变更/工具联查9项/设置4项/打印8项/更多（中止执行/重取BOM材料/附件） | 提交/审核/弃审/手工完工/中止/取消中止/关闭/取消 |
| 表尾 | 制单人/审核人/审核日期/审核时间/打印次数/修改人/变更人/变更日期 | 制单人/创建/更新时间 |
