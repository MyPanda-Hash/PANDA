# T+ 库存核算 6 单据面板 · 真实结构实测（2026-08-14）

| 属性 | 内容 |
|---|---|
| 文档类型 | 场景设计 |
| 适用场景 | 库存与供应链 |
| 维护状态 | 调研基线 |
| 最后整理 | 2026-08-24 |
| 文档导航 | [文档中心](../README.md) |

> 来源：h2t.chanjet.com 演示账套 [012007] 轻MES，账号 tplusdemo12853（无头 Edge + CDP 9222 门户会话）
> 页面：`BAPView/Voucher.aspx?sysId=ST&mId=XXXX&pId=voucherView`（列表/表单同页，点「新增」切换表单态）
> 原始数据：`%TEMP%\pptr\inv\<mId>\`（report.json / 01-body-text.txt / 02-list / 03-form）

## 共同结构

- 网格页签固定成对：**明细（tab_RDRecordDetails）+ 汇总（tab_RDRecordDetails_Sum）**
- 网格右键：定位 / 复制到剪贴板 / 从剪贴板粘贴 / 另存为EXCEL模板 / 批量修改 / 销售订单查询 / 现存量提取 / 更多（Ctrl+V列粘贴）
- 表尾：备注、制单人、代理人、修改人、审核机器人、审核人、审核日期、审核时间、打印次数、是否手工修改过单据号、出入库方向、是否自动生成、创建时间（各单略异）
- 公共工具栏（各单一致）：新增（新增/引入常用单据/设置默认功能）、保存（保存/保存新增/保存为草稿/保存为常用单据/保存打印）、删除、审核、变更、设置（单据设置/移动控件位置/调整控件宽度/工具栏设置）、打印（直接打印/打印/预览/打印模板设置/导出/明细标签打印/打印情况）、更多（复制/放弃 Alt+Z/草稿 Alt+B/导入/下载导入模板/附件/刷新/消息）

---

## ST1001 采购入库单（编号默认 0001-2026-08-0000-0001）

### 工具栏差异项
- 选单：智能选单
- 转换：转成材料出库单
- 协同（空）
- 工具联查：入库调整情况
- 更多额外：重新取价

### 表头（16）
*单据日期、*单据编号、*业务类型、入库类别、供应商编码、*供应商、*供应商简称、匹配来源单号、经手人、验货人、项目、仓库、来源单据、外部单据号、来源单号、销售订单号

### 明细（22 可见列）
*仓库、*存货名称、存货图片、规格型号、*实收数量、*计量单位、实收数量2、计量单位2、计量单位组合、换算率、单价、税率%、单价2、含税单价2、含税单价、金额、含税金额、费用调整、费用金额、现存量、现存量说明、产成品图片

### 汇总（10 列）
仓库、存货名称、存货图片、规格型号、计量单位、实收数量、单价、金额、含税单价、含税金额

---

## ST1002 产成品入库单（编号默认 0003-0000-2026-08-0002）

### 工具栏差异项
- 选单：选产成品入库单（自制加工）、选生产加工单
- 生单：生成产成品入库单（自制退库）、生成补投生产加工单、生成返工生产加工单
- 转换：转成销售出库单
- 工具联查：生产加工情况、退库情况
- 设置额外：智能选单设置

### 表头（8）
*单据日期、*单据编号、*业务类型、入库类别、生产车间、加工单号、仓库、匹配来源单号

### 明细（12 可见列）
*产品名称、*仓库、存货图片、规格型号、智能选单、*计量单位、金额、单价、*实收数量、现存量、现存量说明、图号

### 汇总（7 列）
仓库、产品名称、规格型号、计量单位、实收数量、单价、金额

---

## ST1004 其他入库单（编号默认 IC-2026-08-0000-0001）

### 工具栏差异项
- 选单：其他出库单
- 转换：转换成其他出库单
- 工具联查：其他出库情况
- 设置额外：智能选单设置

### 表头（7）
*单据日期、*单据编号、*业务类型、入库类别、仓库、匹配来源单号、来料客户

### 明细（12 可见列）
*仓库、*存货名称、规格型号、*计量单位、*数量、智能选单、计量单位2、数量2、单价、金额、现存量、现存量说明

### 汇总（7 列）
仓库、存货名称、规格型号、计量单位、数量、单价、金额

---

## ST1021 销售出库单（编号默认 IO-2026-08-0011）

### 工具栏差异项
- 选单：选销售订单
- 生单：生成销售出库单(销售退货)
- 工具联查：销售订单情况、退库情况、出库情况
- 设置额外：智能选单设置

### 表头（9）
*单据日期、*单据编号、*业务类型、退货原因、*客户、*结算客户、匹配来源单号、经手人、仓库

### 明细（19 可见列）
*仓库、*存货名称、*存货编码、规格型号、*计量单位、*数量、智能选单、成本价、税率%、售价、含税售价、销售金额、税额、含税销售金额、折扣金额、现存量、现存量说明、需求令号、退货原因

### 汇总（9 列）
仓库、存货名称、规格型号、计量单位、数量、成本价、售价、销售金额、含税销售金额

---

## ST1022 材料出库单（编号默认 MD-2026-08-0002）

### 工具栏差异项
- 选单：选材料出库单（直接领料）、选材料出库单（自制领料）、选生产加工单、选生产加工单(新增材料)
- 选单转换：选材料出库单（自制领料）、选生产加工单
- 生单：生成材料出库单（直接退料）、生成材料出库单（自制退料）
- 工具联查：生产加工情况、退料情况

### 表头（10）
*单据日期、*单据编号、*业务类型、出库类别、生产车间、领用人、仓库、来源单据、销售订单号、匹配来源单号

### 明细（12 可见列）
*仓库、加工单号、*材料名称、*计量单位、*数量、单价、金额、规格型号、手工确定成本、明细备注、现存量、现存量说明

### 汇总（8 列）
仓库、加工单号、材料名称、规格型号、计量单位、数量、单价、金额

---

## ST1024 其他出库单（编号默认 ID-2026-08-0001）

### 工具栏差异项
- 转换：转换成其他入库单
- 工具联查：其他入库情况、联查设备投放单

### 表头（5）
*单据日期、*单据编号、*业务类型、仓库、来料客户

### 明细（9 可见列）
*仓库、*存货名称、规格型号、*计量单位、*数量、单价、金额、现存量、现存量说明

### 汇总（7 列）
仓库、存货名称、规格型号、计量单位、数量、单价、金额

### 表尾（最全，其余单据按此裁剪）
备注、制单人、代理人、修改人、审核机器人、审核人、审核日期、审核时间、打印次数、是否手工修改过单据号、出入库方向、是否自动生成、创建时间
